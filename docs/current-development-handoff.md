# 当前开发交接与后续优化路线

> 状态：当前开发交接
> 适用版本：6.4.2 补丁发行
> 最后核验：2026-09-15（Asia/Shanghai）
> 最新用户范围：6.4.1 已发布，用户随后同意补齐构建依赖、检查新增开发记录并制作下一补丁 6.4.2，见第 4.21 节。三仓库角色及发布顺序沿用第 9 节，不移动既有标签或替换 6.4.1 资产。ARCH-003、模型证据与 SEC-001 已完成，不自动恢复架构拆分或新增教学界面；物理标准仍待实测。
> 维护方式：这是跨对话继续开发的固定入口。后续交接时直接更新本文件，不另建同类临时报告。

本文件用于让新的 Codex 对话在不依赖旧聊天记录的情况下继续开发。它总结当前功能基线、已经完成的工作、尚未完成的事项、用户已经确认的产品规则、推荐实施顺序、验收门禁和版本库工作流。

## 1. 新对话首先读取什么

开始修改前，按下列顺序阅读和核验：

1. 仓库根目录的 `AGENTS.md`，其规则优先于本文件中的流程建议。
2. 本文件，了解当前阶段、已确认决策和下一步。
3. `docs/README.md`，确认当前资料、参考资料和历史归档之间的边界。
4. `docs/ideas/improvement-ideas.md`，确认仍有效但尚未实施的事项。
5. 与任务直接相关的当前规范：
   - `docs/instrument-modeling/piston-oscillation/physics/piston-oscillation-experiment-spec.md`
   - `docs/architecture/system-architecture.md`
   - `docs/architecture/workbench-heat-capacity-state-authority.md`
   - `docs/prompt-system-ui-spec.md`
6. 当前代码、自动化测试和最近提交。若文档与实际行为冲突，先查明原因，不要直接用旧文档覆盖当前实现。

`docs/archive/` 只用于追溯历史背景。归档文档中的未完成复选框、旧路线图和旧参数不自动恢复成当前任务。

## 2. 时间与版本背景

- 本次交接时间为 2026-09-15，时区为 Asia/Shanghai。
- 当前包版本为 `6.4.2`，包清单与锁文件同步；功能基线为已发布的 `6.4.1`，原 `6.4.0` 本地候选保留为验收历史。
- 当前开发分支为 `codex/piston-oscillation-development`。
- 原功能基线为 `c9bf079 fix: enforce exact piston calculation chain`；阶段 B 修复为 `483f184`。整体架构拆分提交为 `a101f29`，内部检查点为 `1ae473c`、`44b98de`，证据见第 4.13–4.15 节。模型扫描提交为 `ffc9d28`，SEC-001 提交为 `44ccf6e`，终点对照为 `c2776f8`；本次发行从本地候选提交 `915edaa` 开始，最终发行源码以对应仓库的 `v6.4.1` 标签为准。
- 本次发行开始时工作区干净。已只读核验实际远端：`origin` 的主分支和开发分支都是本地历史的祖先，匿名历史可在已有匿名开发提交上继续快进。不得用远端覆盖本地，后续操作仍应重新读取实际引用。
- 本补丁修改前严格 TypeScript、346 个测试文件及生产构建均通过；新增锁文件回归后的最终门禁见第 4.21 节。`js-yaml` 保持 4.3.2；6.4.1 安装包、增量重建和桌面验证见第 4.20 节，固定预览端口仍为 `5174`。
- 阶段 B、活塞只读视图拆分及两批性能修复已完成。PERF-001 已消除标准画布反复订阅、读取尺寸、重置缓冲区和参与祖先尺寸计算的开销；8 秒布局耗时由 2.63–2.73 秒降至 0.73–0.95 秒。帧间隔仍有波动，不能声称全局长帧完全消失；后续修改继续保留同口径性能回归。
- 不要因为缺少旧聊天上下文而重新推翻已经确认的规则；先以本文件和现行测试恢复上下文。

## 3. 用户的总体目标与设计原则

软件是面向气体定律与比热容比实验的教学仿真。当前包含绝热膨胀法、活塞振荡法、理想气体和硬球模型等内容。本轮长期工作的目标不是简单堆功能，而是同时改善：

- 实验流程正确性、物理因果与教学解释；
- 模式切换、参数权威、保存恢复和结果窗口的一致性；
- 大型工作台组件的可维护性；
- 第三方资料、项目原创材料、法律清单和历史文档的可追溯性；
- 自动测试、固定端口预览与人工验收的可靠性；
- 性能稳定性，特别是文件切换、模型加载、模式切换、参数调整和点击反馈。

必须遵守的基本原则：

- 真实物理结果不能通过人工惩罚或多个误差相互抵消来“调对”。
- 官方规格、实测标定、文献先验、几何推算和教学候选参数必须明确区分。
- 项目自己制作的代码、文档、三维模型、图像或录音，记录为“本项目原创”或“项目内部制作”即可，不需要虚构第三方来源；若制作时参考了外部材料，另行记录参考依据。
- 最新本地代码是开发冲突时的首要事实来源。远端出现差异时只获取和比较，不得直接覆盖本地。
- 当前测试阶段允许舍弃旧的活塞计算测试文件。为旧文件兼容而破坏最新正确逻辑不可接受；但这不是取消所有生产兼容性的通用授权。

## 4. 已完成的主要工作

### 4.1 第零阶段：资料与仓库整理

- 建立 `docs/README.md` 作为统一资料地图。
- 将已完成、被替代或只供追溯的方案移入 `docs/archive/`，不再维护旧正文。
- 从旧方案和记录中提取仍有效的想法，统一写入 `docs/ideas/improvement-ideas.md`。
- 按实验、物理模型、架构、验证、法律、音频和本地资料等职责重新分类。
- 删除已确认无保留价值的临时产物；保留不可轻率删除的用户材料。
- 建立项目原创材料的来源/权利记录以及生成型第三方与法律清单。

### 4.2 架构与状态权威整理

- 绝热膨胀法实验组已成为目标次数、试次、轨迹、进度、计算、评分和结果的权威来源。
- 旧工作区、旧文件信封和早期 V3 投影迁移已收口到兼容边界。
- 旧 `history` 面板键已从当前状态和不可达界面代码中移除，仅在读取旧数据时映射。
- 工作台的默认文件、跨模式调零、自由实验组生命周期、参数和文件联合类型等状态已迁出。
- `workbenchState.ts` 已成为 300 行的兼容重导出门面，仓库内部调用方已改为直连职责模块。
- 已拆出结果展示、理想气体验证、实时数据、绝热膨胀材料标签页、计算协调、重置、教学运行时和自由操作证据等模块。
- 结果窗口已统一为单窗口内的浏览器式标签页；其打开、关闭、追加和撤销边界已有明确合同。

### 4.3 两种实验的模式与学习流程

- 两种实验统一采用：演示模式不保留、未完成引导不保留、已完成引导可复查、自由模式保留到完整重置。
- 首次使用时可分别确认是否熟悉两种实验；不熟悉时按“绝热膨胀法 → 活塞振荡法”逐一解锁演示、引导和自由模式。
- 引导与演示之间切换不会破坏自由模式内容；完整重置后自由模式等同于新的实验文件。
- 参数只允许在自由模式修改，且只能在实验尚未发生不可逆操作时修改。
- 已统一自由模式状态权威、实验组生命周期和关键保存恢复规则。

### 4.4 Real / Ideal 与气体类型

用户已经明确：这里的 Real / Ideal 指“实验过程条件”，不是理想气体/真实气体状态方程的切换。

- 绝热膨胀法与活塞振荡法均支持空气和氦气。
- Ideal 是预置参数组：关闭固有环境扰动、随机观测误差、热交换、机械损耗和人为尾部修饰，并使用精确的物理终点或坐标。
- Ideal 只保证实验条件理想，不替用户消除错误操作；用户操作错误仍会得到不准确结果。
- Real 和 Ideal 共用相同的手动数据处理、框选、拟合和逐步计算流程。
- Ideal 不参与数值评分，但可以显示过程诊断与教学反馈；不得把诊断伪装成评分。
- 文件条件在不可逆实验动作前可以修改；完整重置后重新开放修改。

### 4.5 活塞振荡法方案、计算和最近缺陷修复

活塞振荡法已经完成下列功能：

- Real / Ideal × 空气 / 氦气四种组合接入记录、框选、拟合、计算、回顾和报告链。
- 三组高度实验、连续采样、过程证据、错误操作结果与 Real 评分。
- 关闭电源后的引导流程会进入数据处理/二级计算窗口，不再停在错误的“设置参数”提示。
- 引导框选最低要求从 3 个周期调整为 2 个周期。
- 周期识别不再简单收集所有局部极值，而是以自相关主周期估计结合平滑后的主峰/主谷识别；局部小峰不得被当成独立周期，含糊数据应拒绝而不是凑数。
- 已增加相应回归测试，保护断电转场、最少周期数、局部微峰和含糊波形。

最新计算合同必须完整保留：

- 不使用“容差内算正确”。正确答案必须与规定精度下的参考数值一致。
- 后台正确答案必须模仿用户按界面数据逐步计算，不得偷偷使用未显示的高精度底层值。
- 拟合坐标统一使用显示后的 `T²`（5 位有效数字）和高度 `h`（4 位有效数字）。
- 已知量采用界面给出的精度：质量 `m` 保留 4 位小数，直径 `d` 保留 1 位小数（mm），压强 `P` 保留 3 位有效数字。
- 斜率保留 5 位有效数字；截面积 `A` 和比热容比 `γ` 保留 4 位有效数字；相对误差保留 3 位有效数字。
- 相对误差使用已经按界面规则舍入的 `γ` 和界面显示的参考 `γ` 计算。
- 表格、图形、拟合和后续公式必须使用同一套舍入后坐标。
- 计算结果内部版本已经升级；旧版本保存的计算结果不得被静默当作当前答案复用。

### 4.6 2026-09-05：全新活塞引导验收与两项修复

本批只处理阶段 B，未开始 ARCH-003，未修改包版本、物理参数、工作台状态权威或撤销规则。

发现并修复：

1. 操作说明暂时禁用螺钉操作镜后，重新启用时显式传入的 `raycast={undefined}` 会覆盖 Three.js 的命中方法，普通鼠标拖动触发 `object.raycast is not a function`。现在显式恢复 `Mesh.prototype.raycast`；新增真实网格及 R3F 属性切换测试，覆盖反复禁用、启用和射线命中。
2. 显示周期 `0.03250 s` 的平方为精确十进制 `0.00105625`，五位有效数字应舍为 `0.0010562`；二进制乘法却产生 `0.0010562500000000001`，导致旧界面显示 `0.0010563`。现在先做精确十进制乘法，再执行五成双舍入，周期结果直接保存显示值。新增中点舍入、完整周期提交及陈旧平方恢复测试。

周期结果内部版本为 2，拟合及公式内部版本为 v3；包版本仍为 6.4.0。读取旧 v2 拟合或计算时，保留原始记录及有效周期读数，清除下游拟合与计算并要求重新拟合，避免复用错误平方值。无容差判定。

修复后重新新建 `Piston Oscillation - 003`（创建于 10:52:53），通过操作界面完成调高、锁紧、接管、旋松、按压、释放和三次保存。每组采集 501 点、0.500 s；采样率为 1000 Hz，下降触发阈值为 120 kPa。持续按压通过浏览器指针与键盘事件驱动实际交互和物理模型，未注入测量或答案状态。

| 目标高度 / mm | 显示 t₁ / s | 显示 t₂ / s | 主周期 N | 显示 T / s | 显示 T² / s² |
| --- | --- | --- | --- | --- | --- |
| 80.00 | 0.090 | 0.239 | 4 | 0.03725 | 0.0013876 |
| 70.00 | 0.031 | 0.101 | 2 | 0.03500 | 0.0012250 |
| 60.00 | 0.030 | 0.095 | 2 | 0.03250 | 0.0010562 |

- 第一幅窄选区的 1 个周期被明确拒绝。最终第一幅选区内存在额外局部转折，主极值仍为 9 个、周期仍为 4；未把局部小峰计为主周期。自动测试同时覆盖含尾部小峰及周期不稳定的曲线。
- 三组周期提交后，点击下一步回到电源关闭步骤；实际按下电源键后，正常进入离线拟合与计算，未回到参数设置或重复要求断电。
- 由界面已显示的三对坐标独立使用十进制运算重算 OLS，得到显示斜率 `60.343 m/s²`、截距 `−0.0037955 m`，与拟合图一致。
- 只使用显示已知量 `m = 0.0485 kg`、`d = 32.5 mm`、`P = 1.01 × 10⁵ Pa`，以及前一步已舍入的结果，逐步得到 `A = 0.0008296 m²`、`γ = 1.379`、`Eᵣ = 1.50%`。三项全部与后台参考答案相等。
- `T = 0.03724`、`A = 0.0008295`、`γ = 1.378`、`Eᵣ = 1.49%` 均精度合规但数值错误，界面全部拒绝；使用正确值再次提交后通过，未使用“查看并继续”。
- 完成说明收起后，退出并重新进入引导、切换文件、打开只读回顾均正常。11:06:24 刷新页面后，原始记录、周期结果及拟合对象与刷新前逐项完全一致；正确答案、错误尝试和只读状态保留。
- 清理本轮创建的失败重试文件 002，并验证删除动作的撤销和重做；完整验收文件 003 的数据处理对象未改变，原有文件 001 未删除。
- 最后一次代码修改后的检查：`npm.cmd exec tsc -- --noEmit --noUnusedLocals --noUnusedParameters`、`npm.cmd test`（297 文件）、`npm.cmd run build` 均通过，11 个 JavaScript 构建分块符合输出门禁；本批未改依赖，`npm.cmd audit --omit=dev` 为 0 个漏洞。固定端口浏览器本轮无 console error/warn。

范围限制：本轮浏览器证据覆盖活塞新建、调参、交互、文件切换、引导退出重开、结果回顾和刷新，未观察到明显点击或动画卡顿；各模型完整性能路径仍应在 ARCH-003 批次重复。浏览器无桌面科学 PDF 导出桥接，导出数据由现有测试覆盖；未制作 NSIS 安装包或验证桌面发布。

### 4.7 2026-09-05：ARCH-003 活塞只读视图状态拆分

- 新增 `src/features/workbench/workbenchPistonOscillationViewState.ts`（130 行），集中选择当前文件的 Demo/Guide/Free 模式、自由模式计划入口、参数栏可用性、电源显示、数据处理、过程回顾和计算窗口状态。
- 输入由工作台文件及原有瞬态窗口请求组成；输出复用原 Guide、Free、数据处理和计算会话引用。模块仅有类型导入，不增加 React Hook、订阅、状态副本、规范化或存档写入。
- 原有有效参数 `useMemo`、实时压力通道、计时器、交互处理、模式转换、持久化与撤销继续留在原位置；精确计算和物理逻辑没有修改。
- 主组件实测由 25,315 行降至 25,216 行。此前交接的“24,268 行”为过期统计，本批已修正；`workbenchState.ts` 仍为 300 行兼容重导出。
- 新增 63 组冻结输入测试，覆盖四种文件类型、跨文件旧标记、模式优先级、Guide 断电与计算入口、完成结果回顾、Free 重采集、窗口抑制，以及活动 Free 会话不得回退到旧 Guide 答案。检查输入完全不变和关键嵌套引用身份；旧源码正则检查改为验证主组件接线，决策规则由直接行为测试覆盖。
- 严格 TypeScript、298 个测试文件的全量测试和生产构建均通过；构建为 2,271 个模块、11 个 JavaScript 分块，输出大小门禁通过。

5174 浏览器功能证据：

- 新建独立临时活塞、标准模拟、理想气体、绝热膨胀文件，完成模型加载、文件切换、运行/暂停/停止或模式切换。活塞实际点击电源后启动采集界面，正确填写 1000 Hz / 120 kPa 后进入高度调整。
- 活塞自由模式计划为 80 / 70 / 60 mm；环境压强调至 102 kPa 后，在 Guide、Demo、Free 间切换并恢复，自由模式参数保留。Guide 参数栏继续按原规则拒绝打开。演示暂停、继续、终止均响应。
- 理想气体目标温度调至 0.80，展开/收起高级设置、切换 P-V/P-T 并运行，成功记录 P-T 点；绝热膨胀自由模式环境温度调至 26 ℃，切换演示并返回后保留。
- 跨模型切换后，保留文件 `Piston Oscillation - 003` 的三组 `savedMeasurements` 和整个 `dataProcessing` 对象与改动前 JSON 逐项完全一致。最终刷新仍显示三组数据、`T² = 0.0010562`、`A = 0.0008296`、`γ = 1.379`、`Eᵣ = 1.50%`，完成结果为只读；最后刷新会话的 console error/warn 均为 0。
- 本轮创建的主预览临时文件 004、标准 001、理想气体 001、绝热膨胀 001 已清理；删除活塞临时文件的撤销与重做通过。主预览保留原文件 001 和上一批完整验收文件 003。

性能证据及未完成项：

- 同一活塞自由模式场景改动前后各采样 3 秒，帧间隔 P95 均为 8.4 ms，均无超过 50 ms 的帧。
- 演示与标准模拟运行时观察到偶发长帧，最初一次演示启动窗口最大约 275 ms。随后用 5184 临时开发预览在内存中加载 `483f184` 的原主组件，未覆盖本地文件；已检查返回源码确实含旧内联判断。
- 排除视口不一致的早期对比。统一 1280×720 后，连续两个 3 秒窗口中，旧提交 P95 为 33.3 / 8.5 ms，当前代码为 41.5 / 16.6 ms；当前代码第二窗口含一帧约 91.6 ms。两侧实验文件数量、保存数据和缓存负载仍不相同，不能据此确认或排除本次拆分引入性能回归。
- 因此本批在本地提交处停止，下一批先统一工作区数据、视口和运行阶段并采集性能记录，查明长帧来源；证据充分后再进入绝热膨胀模式与持久化副作用拆分。未做物理标定或桌面发行验收。
- 临时对比实验、5184 服务和临时配置已清理，视口覆盖已恢复。自动审批拒绝递归删除本轮生成的 `node_modules/.vite-arch003-baseline`，仅返回 `blocked by policy`；该目录被 Git 忽略，未提交。预览服务重新确认在 5174 运行。

### 4.8 2026-09-05：移除活塞氦气预设说明框

- 根据用户截图红框，移除自由模式、真实氦气条件下气体选择器下方的说明文字和整个提示框；同步清理三种语言的对应文案及要求显示该提示的旧测试断言。
- 气体选择、参数值、物理计算和保存逻辑不变。5174 当前文件的真实氦气参数栏已确认不再显示该框，后续参数自然上移，浏览器控制台无错误或警告。
- 严格 TypeScript、298 个测试文件的全量测试及生产构建均通过；版本保持 6.4.0，仅本地提交。
- 用户明确要求暂不继续下一批修改；第 4.7 节的性能待查项及阶段 D 保留，等待用户重新要求继续。

### 4.9 2026-09-06：性能复核与隐藏操作镜暂停绘制

用户重新要求进入下一阶段后，先收口第 4.7 节的性能证据缺口。本批没有继续迁移工作台职责，也未修改绝热膨胀模式与持久化逻辑。

对照方法与发现：

- 使用独立浏览器上下文和同一份测试工作区快照，包含标准模拟、理想气体、绝热膨胀、活塞各一个新文件；每次恢复相同保存数据和会话数据。统一 Chrome 152、1280×720、DPR 1，显式保持测试页面在前台。未改动主预览中原有的 001 / 003 文件。
- 通过 5184 的临时 Vite 配置在内存中读取旧提交源码，再于 5174 的独立测试页面替换对应模块响应；未覆盖工作区源码。逐次检查实际返回模块和运行状态，排除未命中源码替换的试跑；CPU 采样记录不混入无采样器的帧间隔对照。
- 最初比较拆分前 `483f184` 的主组件与 `c55c734`：三次冷启动的稳定段 P95 分别为 25.1 / 33.1 / 25.1 ms 和 33.2 / 41.8 / 25.0 ms，启动长帧在两侧均出现，不能从这组波动直接判定选择器拆分的性能影响。采样热点涉及 Three.js 渲染、程序选择和包围盒计算，另一次启动记录含音频上下文初始化；这些是观测到的开销，不是全部长帧的完整归因。
- 找到可直接验证的问题：操作镜已经隐藏且首帧准备完成，Canvas 仍为 `demand`，演示更新会继续触发它绘制。修复前单独观察 5 秒，隐藏镜绘制增加 125 帧；修复后同条件增加 0 帧。

实现边界：

- `pistonOscillationOperationMirror.ts` 增加纯绘制策略：两个常驻模型就绪且初始视图完成首帧后，只有完全隐藏时切换为 `never`；显示中和原有退出动画期间仍为 `demand`。
- `PistonOscillationInteractionWorkspace.tsx` 只改操作镜 Canvas 的 `frameloop` 接线。主画布、常驻模型、相机、物理步进、交互状态、保存和撤销均保持原实现；工作台主组件仍为 25,216 行。
- 补充初始准备、反复打开/退出/隐藏的策略测试，并保留真实网格的射线命中恢复回归。

最终对照仅替换 `c55c734` 的操作镜工作区模块，主组件和其余源码均使用当前版本，隔离本次修复。两侧先演示 6 秒、终止并等待 1 秒，再记录 8 秒；主画布 563×444、操作镜 165×221。

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 8 秒内隐藏操作镜新增绘制次数 | 207 | 0 |
| 0–2 秒帧间隔 P95 | 41.7 ms | 41.7 ms |
| 2–8 秒帧间隔 P95 | 25.0 ms | 24.9 ms |
| 2–8 秒最大帧间隔 | 66.7 ms | 83.3 ms |
| 2–8 秒超过 50 ms 的帧数 | 1 | 1 |

因此可以确认消除了隐藏镜的无效绘制，不能据此声称整体动画全面提速或偶发长帧已经消失。较早同时切换旧主组件与旧操作镜的一组对照为 200 次 → 0 次、稳定段 P95 33.3 → 24.9 ms；最终结论以上表隔离操作镜的对照为准。

5174 功能回归与验证：

- 连续三次退出再进入活塞聚焦，Canvas 和场景身份保持不变；退出动画期间仍绘制，隐藏后停止，重开后恢复。Shift 可切换刻度正交视角和螺钉透视视角，首帧就绪正常。重新打开后的螺钉悬停、按下、圆周拖动有效，实际紧固进度从 0 增至 0.3324，未注入实验状态。
- 活塞自由计划 80 / 70 / 60 mm、氦气、102 kPa 在 Guide / Demo / Free 往返后保留；Guide 仍拒绝打开参数栏。演示暂停、继续、终止正常，已移除的氦气说明框没有恢复。
- 标准模拟可开始、暂停、停止；理想气体可展开/收起高级设置、切换 P-V / P-T，并在目标温度 0.80 下记录一个 P-T 点；绝热膨胀自由模式温度 26 ℃在演示往返后保留。
- 刷新后按现行规则重新进入自由模式，绝热膨胀 26 ℃、活塞氦气 / 102 kPa / 三个计划高度均恢复；活塞模型重新加载后，操作镜仍可打开、就绪和关闭。每次性能对照页面的 pageerror 为 0，最后一次恢复回归的 console error/warning 与 pageerror 均为 0。
- 严格 TypeScript、46 个活塞相关测试文件、全量 298 个测试文件及生产构建通过；构建 2,271 个模块、11 个 JavaScript 分块，输出门禁通过。沿用 2026-09-05 的生产依赖审计；包版本 6.4.0，未做桌面发行、上传或发布。
- 独立测试上下文和 5184 对照服务已关闭。自动审批拒绝清理 `output/playwright/` 下的本批 `arch003-compare.config.ts`、`arch003-perf.js`、`arch003-seed.json`、`arch003-mirror-visible.png` 和 `arch003-vite-cache/`，仅返回 `blocked by policy`；命令未执行，全部暂留且被 Git 忽略，不纳入提交。第 4.7 节已经被拒绝清理的旧缓存未触碰；5174 预览继续保留。

剩余范围与下一个断点：同一快照下另测标准模拟，拆分前主组件 / 当前代码的 2–8 秒 P95 为 108.3 / 100.0 ms，超过 50 ms 的帧均为 38 个，最大帧间隔为 182.9 / 158.3 ms。两侧画布均为 464×332；长动画帧记录含反复的强制布局开销，并有 `SimulationCanvas.updateSize` 的尺寸读取记录，尚未完成根因隔离。该问题在旧代码也存在，不能归为本次操作镜修复引入，但仍是全局性能风险。下一批以 `PERF-001` 定位标准实时界面布局/尺寸更新为起点，收口后再进入阶段 D；不要把物理步进或采样频率的降级当作渲染优化。

### 4.10 2026-09-06：PERF-001 标准画布尺寸与布局修复

本批保持工作台主组件、物理步进、采样频率、统计与计算精度、文件状态、持久化和撤销逻辑不变。未开始阶段 D。

已隔离的两项开销：

1. `SimulationCanvas` 的尺寸 effect 依赖每次粒子或相机更新都会变化的 `draw`。它反复重建 ResizeObserver、同步读取布局，并收到新的初始尺寸通知。容器实际为 464.2778×332.4444 CSS 像素，直接与整数画布宽高比较还会反复清空缓冲区。一次 8 秒诊断中，订阅重建 206 次，尺寸读取和宽高重写各 366 次；最终修复后同条件均为 0，正常粒子绘制继续进行。
2. 工作台画布以普通流中的百分比尺寸参与祖先布局。仅稳定订阅仍留下较多布局开销；让 canvas 在已定尺寸的视口内绝对定位后，布局耗时明显下降。对照中视口矩形、canvas 矩形和 464×332 缓冲尺寸一致。曾检查收起的参数栏滚动条，但单独隐藏参数栏不能解释主要开销，最终未修改参数栏布局。

实现与直接测试：

- 新增 `src/components/simulationCanvasResize.ts`，集中管理一次尺寸订阅、同帧通知合并、窗口/分辨率变化、整数缓冲尺寸、卸载取消及迟到回调守卫。沿用原来的像素尺寸截断方式，不改变投影公式。
- `SimulationCanvas.tsx` 用 layout effect 更新当前绘制引用，尺寸监听只在挂载时建立；粒子与相机更新仍走原有绘制 effect。工作台 CSS 只为 `.simulation-canvas-workbench-frame > canvas` 增加绝对定位，视口和覆盖控件保持原位置。
- 新增直接行为测试，覆盖小数尺寸、重复通知、最新绘制回调、DPR 往返、同缓冲尺寸但不同 DPR、隐藏/恢复、卸载与迟到回调；未增加数据权威或写入路径。

受控对照使用同一四文件工作区快照、Chrome 152、1280×720、DPR 1、前台页面、默认标准参数。每次独立恢复上下文，等待初始画布稳定后启动，采样 8 秒；测试和构建不与性能采样并行。修复前的两个模块响应在改动前保存于浏览器内存，后续旧代码对照只替换这两个响应，不覆盖本地文件。CPU 采样、诊断包装和临时 CSS 排查均不计入下表。

| 独立运行 | 8 秒布局耗时 / s | 2–8 秒帧间隔 P95 / ms | 2–8 秒最大帧间隔 / ms | 2–8 秒超过 50 ms 的帧数 |
| --- | --- | --- | --- | --- |
| 修复前 1 | 2.727 | 75.0 | 100.0 | 27 |
| 修复前 2 | 2.668 | 91.7 | 116.6 | 29 |
| 修复前 3 | 2.632 | 100.0 | 133.4 | 34 |
| 修复后 1 | 0.728 | 108.3 | 200.0 | 19 |
| 修复后 2 | 0.950 | 33.4 | 50.0 | 0 |
| 修复后 3 | 0.918 | 41.7 | 74.9 | 5 |
| 修复后 4 | 0.900 | 50.0 | 108.3 | 8 |

布局耗时中位数下降约 66%，三次后续复测的稳定段 P95 为 33.4–50.0 ms；第一轮修复后仍有 200 ms 长帧，必须一起保留，不能只挑最快一轮或把布局开销下降等同于所有卡顿消失。所有对照页面均无 pageerror。

5174 浏览器回归：

- 标准画布旋转、滚轮缩放、右键平移有效，恢复默认视角后绘制内容一致。右侧参数栏、中央分隔条、控制台展开/收起，以及 1280×720 / 1440×900 窗口切换后，缓冲尺寸与实际容器尺寸一致。
- DPR 1 / 2 随窗口变化时尺寸与绘制恢复正确。单独改变 DevTools 模拟 DPR 不会分发浏览器的 resize / 媒体查询 change 事件，因此不作为真实跨显示器事件验证；媒体查询订阅和反复变更由直接测试覆盖，未做实体多显示器验证。
- 标准容器边长从 15 改为 16，撤销回到 15、重做到 16；切换文件及刷新后仍为 16。开始和暂停时数值与粒子更新正常。结果窗口可打开三个标签页、切换数据表、关闭并撤销恢复，画布尺寸没有陈旧值。
- 理想气体高级设置正常展开滚动与收起；目标温度 0.80 可启动及暂停。标准、理想气体、绝热膨胀和活塞文件反复切换，模型正常加载。
- 绝热膨胀演示推进到开阀步骤，可终止并进入/退出引导，建立三次自由实验组后刷新恢复。活塞演示正常开机并进入采集参数步骤，可终止并进入/退出引导；80 / 70 / 60 mm 自由计划建立后刷新仍显示第 1 / 3 次、目标 80 mm。没有重做已通过的完整活塞三组精确计算批次，相关全量回归继续通过。
- 交互回归 pageerror 为 0，最后一次文件与结果窗口恢复回归的 console error/warning 为 0。科学 PDF 桌面桥接、NSIS 发行与实体设备没有在浏览器中重验。
- 连续 DPR/焦点诊断的上下文曾出现缩放容器滚动偏移，刷新后恢复；另起相同快照的修复前/后上下文直接进入活塞引导，两侧工作台均保持 (0, 0, 1280, 720)，没有复现。该一次性观察未完成稳定复现，不归因于本次画布修复，也不扩大为本批界面行为改动；后续模式回归留意。
- 严格 TypeScript、11 个相关测试文件、全量 299 个测试文件、生产构建均通过；构建 2,272 个模块、11 个 JavaScript 分块，输出门禁通过。版本仍为 6.4.0，未上传、发布或改依赖。
- 本批独立测试上下文、内存诊断和两张临时截图已清理；没有另起预览端口。前批被拒绝清理的 `arch003-*` 文件及缓存未删除，本批只读复用其中的测试快照。5174 预览继续保留，原有用户文件未被测试覆盖。

PERF-001 的确定性尺寸与布局缺陷在本批收口；偶发长帧继续按第 5.1 节监视。下一批进入阶段 D，独立拆分绝热膨胀模式交互与持久化副作用，并保留同样的功能和性能门禁。

### 4.11 绝热膨胀模式入口与保存触发协调拆分（2026-09-06）

本批对应阶段 D，保持独立架构范围：

- `workbenchHeatCapacityModeActivation.ts`（153 行）负责纯会话选择：新建/恢复 Demo、Guide、Free，校验 Demo 检查点，选择文件激活投影及教学重置确认条件。
- `workbenchHeatCapacityModeActions.ts`（162 行）负责动作时序：从 Explore 进入、退出正式模式、提交切换请求、文件激活，以及下一帧保存触发。通过回调使用原文件引用、模式状态机、场景准备、Demo 时钟和 UI 所有者，构造时不做工作。
- 确认后重新读取当前文件与模式；保存帧执行时重新检查退出/故障门禁并读取最新保存函数。普通事件只安排 refresh，切换请求仍在同一回调内先 refresh 再 flush。Explore 返回 undefined 检查点覆盖，正式模式保留显式 null 的语义。
- 统一离开策略、实际场景过渡与导航释放、检查点采集、持久化调度/V3 写入、撤销边界及实验物理计算没有改写。模式会话 schema 3、运行快照 schema 2 与版本 6.4.0 保持不变。主组件由 25,216 行降至 24,981 行，减少 235 行；未新增状态权威或逐帧领域工作。
- 新增两个直接行为测试文件，覆盖 Demo/未完成 Guide 的丢弃、完成 Guide/Free 的恢复、重置后不复活旧现场、无效 Demo 回退、活动文件变化后的确认、运动等待/取消切换、提交与 UI/保存的顺序，以及延迟保存时退出/故障门禁。原源码合同改为检查专用模块与主组件接线，没有放宽物理、持久化或计算断言。

5174 浏览器回归使用独立上下文和测试快照，未覆盖用户已有文件：

- 演示正常推进至开阀/调零步骤；暂停、取消切换、确认转入全新引导正常。未完成引导切换文件再返回为 Explore，再进入从第 1 / 16 步开始；退出引导正常。
- Free 三次计划可在文件往返后恢复。环境温度从 25 改为 26 ℃ 后依照现行行为重建计划，再往返/刷新，进入 Free 后仍保留三次计划与 26 ℃。此路径刷新先回 Explore、该侧栏参数操作不新增撤销项，均在改动前模块的独立上下文中复现；不是本批新增行为。恢复默认确认后回到 25 ℃。
- 完成 Guide 夹具来自现有 `heatCapacityModeBoundaryRefactor.test.ts` 的真实领域推进、U0/U1/U2 记录与关机完成流程，经当前 V3 写入独立工作区；不是本批重新人工采集。记录为 U0=0.0、U1=115.0、U2=31.7 mV。计算中途刷新保留已确认步骤，完成计算后切换文件、再进 Guide、打开“数据与结果”及刷新，仍显示完成状态与 γ=1.391。原绝热膨胀计算规则未改变，也未改变活塞严格显示值计算链。
- 标准容器边长 15→16、撤销至 15、重做到 16，运行与暂停正常；理想气体高级设置展开/收起正常；四类文件与模型加载、活塞演示/引导进入及退出正常。场景与结果窗口截图没有重现前批记录的整体偏移。浏览器 pageerror 为 0。
- 发现现有窗口历史边界：打开“数据与结果”后整体关闭“实验资料与结果”，撤销菜单仍指向原“打开标签页”；撤销再重做后窗口仍关闭。使用修改前 `eee6f7f` 主组件响应与本批响应，分别从同一完成夹具复现，结果一致。已登记 HC-UX-003；本批保持交互不变，不能声称这条关闭/撤销/重做链已经修好。

性能对照沿用第 4.10 节的四文件快照、1280×720、DPR 1、前台 Chrome、默认标准参数和 8 秒采样口径；测试/构建不与性能采样并行。修改前主组件响应在编辑前保存于内存，对照只替换浏览器模块响应，不覆盖本地源码。

| 独立运行 | 8 秒布局耗时 / s | 2–8 秒帧间隔 P95 / ms | 最大帧间隔 / ms | 超过 50 ms 的帧数 |
| --- | --- | --- | --- | --- |
| 修改前 1 | 0.850 | 50.1 | 116.6 | 15 |
| 修改前 2 | 0.872 | 49.9 | 75.1 | 7 |
| 修改前 3 | 0.882 | 50.0 | 116.7 | 10 |
| 修改后 1 | 0.677 | 124.9 | 202.0 | 22 |
| 修改后 2 | 0.848 | 50.1 | 125.0 | 12 |
| 修改后 3 | 0.890 | 41.6 | 116.6 | 7 |

布局中位数 0.872→0.848 s，脚本耗时中位数 3.201→3.251 s；后两次帧间隔与基线相近，没有观察到持续性的全局开销上升。首轮修改后仍有约 202 ms 长帧，保留该证据，继续按第 5.1 节监视，不能宣布所有卡顿消失。

严格 TypeScript（含未使用符号）、相关测试和生产构建通过；构建 2,274 个模块、11 个 JavaScript 分块。全量验证记录更正：下一批对 `1383c94` 重跑时，实际停在仍要求旧架构文档措辞的断言，不能沿用本节原先“最终全量 301 个测试文件通过”的结论；修正及重新全量通过的证据见第 4.12 节。只本地提交，不推送、不发布、不修改版本号。测试上下文与内存对照已清理，5174 原预览继续运行。自动审批拒绝删除本批 4 个临时文件，仅返回 `blocked by policy`：`output/playwright/stage-d-guide-fixture.ts`、`stage-d-completed-guide.json`、`stage-d-free.png`、`stage-d-completed-guide.png`。这些忽略文件保留且不进入提交；未改用其他方式删除。前批受保护的 `arch003-*` 文件也未删除。

阶段 D 的历史批次在此收口。HC-UX-003 后由第 4.12 节完成，ARCH-003 整体后由第 4.15 节完成；这里不再单独产生新的拆分待办。进入阶段 E 的物理标定前，仍须按第 6 节先报告证据与选项，由用户决定方向。

### 4.12 绝热膨胀结果窗口整体关闭的撤销修复（2026-09-06）

本批处理 HC-UX-003，不扩大为窗口系统或模式会话改写：

- 修改前在独立浏览器会话复现：完成 Guide、打开“数据与结果”、整体关闭窗口后，撤销仍指向打开标签；撤销和重做均不能重新显示结果。原因是整体关闭回调漏记历史。
- 整体关闭现在先记录 `presentation` 布局快照，再关闭窗口；已有的空窗口和文件类型门禁保留。补充简繁中文操作日志说明。主组件只增加 3 行，现为 24,984 行。
- 撤销/重做沿用现有布局恢复和保存调度，只恢复标签、活动页和窗口布局；不回滚实验进度、模式会话、物理状态、计算或实验组权威。标签切换仍不增加撤销项，单标签关闭仍使用原入口。
- 新增 `workbenchHeatCapacityMaterialsHistory.test.ts`：通过 TypeScript AST 取得实际 UI 回调与历史函数，使用同步界面端口执行连续操作；不是仅检查源码中是否出现函数名。修复前准确失败于“打开和整体关闭应产生两个独立历史项”，修复后覆盖 Guide/Free、两次撤销/重做、重做分支替换、活动标签和顺序、末标签关闭、跨文件撤销、空操作，以及更新后的领域状态与对象引用不被布局恢复覆盖。
- 启动基线时发现 `workbenchHeatCapacityStateBoundary.test.ts` 仍强制匹配“下一大改动断点转向 25,291 行”的旧文档。该断言改为核对已完成的架构边界，不冻结历史行数。仅修正断言后原有 301 个测试通过，再进行产品修复；同步更正本交接与架构文档的过期当前行数和后续提示词。

5174 浏览器验证均在独立工作区进行，未覆盖用户文件：

- 复用第 4.11 节经领域流程生成的完成 Guide 夹具，并在界面实际完成四步计算；没有重新人工采集。窗口整体关闭、菜单撤销/重做、快捷键连续撤销/重做全部通过。
- 从“窗口”菜单追加实验指引，切换活动页不增加历史。整体关闭后撤销恢复原有两标签顺序和“数据与结果”选中页；关闭非活动标签与关闭最后一个标签也能撤销恢复。
- 在标准文件上重做绝热膨胀窗口关闭，不改变当前标准文件；返回绝热膨胀再撤销可恢复原窗口。分别在撤销恢复后、重做关闭后刷新，依现有规则先回 Explore，再进 Guide，恢复对应的打开/关闭布局。结果仍为 U0=0.00、U1=115.00、U2=31.70 mV，γ=1.391、相对误差 0.63%。
- 新建 Free 三次计划，打开三标签窗口、选中“数据与结果”、整体关闭、撤销、重做通过；模式保持 Free。对运行中领域提交的引用保留由直接测试覆盖，本批未另做 Free 三次完整物理采集。
- 四类文件切换和模型加载正常；标准参数 15→16→撤销 15→重做 16，开始/暂停正常；理想气体高级设置展开/收起正常；绝热膨胀演示可暂停与终止，引导进入/退出正常；Free 温度 25→26 ℃ 后文件往返仍保留 26 ℃；活塞 80/70/60 mm 自由计划入口正常，已删除的候选预设说明未恢复。窗口撤销后的内存截图已目视复核，未见整体偏移或遮挡。所有验收上下文 pageerror 为 0。

性能对照沿用第 4.10 节口径：独立四文件快照、1280×720、DPR 1、前台标准实验、8 秒采样，帧间隔统计 2–8 秒；不与测试或构建并行。修改前模块只在独立浏览器响应中替换，未覆盖本地源码。

| 独立运行 | 8 秒布局耗时 / s | 帧间隔 P95 / ms | 最大帧间隔 / ms | 超过 50 ms 的帧数 |
| --- | --- | --- | --- | --- |
| 修改前 1 | 0.837 | 66.7 | 150.0 | 20 |
| 修改前 2 | 0.748 | 50.0 | 241.7 | 11 |
| 修改前 3 | 0.826 | 41.6 | 49.9 | 0 |
| 修改后 1 | 0.706 | 75.0 | 174.9 | 23 |
| 修改后 2 | 0.803 | 50.0 | 92.2 | 11 |
| 修改后 3 | 0.848 | 33.5 | 82.9 | 5 |

布局中位数 0.826→0.803 s，脚本耗时中位数 3.206→3.098 s，P95 中位数均为 50.0 ms。未观察到持续开销上升，但两侧仍有偶发长帧；本批不宣称消除全部卡顿。

严格 TypeScript、5 个相关测试、全量 302 个测试文件和生产构建通过；构建 2,274 个模块、11 个 JavaScript 分块。版本仍为 6.4.0，未改依赖或存档版本，不推送、不发布。测试上下文和浏览器内存对照已清理，本批没有新增磁盘临时产物，也未尝试删除此前被阻止清理的文件；5174 原预览继续运行。HC-UX-003 在此本地提交收口，后续 ARCH-003 整体完成记录见第 4.15 节。

### 4.13 ARCH-003 整体拆分：内部检查点 1（2026-09-06）

以下保留内部检查点的当时状态，最终整体完成见第 4.15 节。用户已将交付断点调整为整体架构拆分完成。当前合同见 [Workbench 界面组合合同](architecture/workbench-ui-composition.md)。下面是内部保存点，不能把这一小批当作整体交付，也不进入物理标定。

- 从 24,984 行主组件迁出教学只读判断与三语提示、面板/参数/日志呈现、纯遮罩几何及独立 DOM 测量、教程文件工厂、历史快照与动作、窗口动作、保存快照与请求。主组件暂为 21,880 行。
- 快照与历史直接测试覆盖显式空检查点、延迟捕获、待恢复会话归属、三个历史范围和跨文件窗口撤销；原窗口关闭前捕获与更新器内重规划顺序不变。教学判断 24 个函数经 AST 比较与上一提交函数体一致。
- 89 份主入口源码合同逐项核对实际模块归属，没有拼接整个目录模拟旧入口；新增 5 个直接测试文件。
- 基线严格 TypeScript、302 个测试文件和构建通过。本检查点严格 TypeScript、307 个测试文件、生产构建通过；11 个 JavaScript 分块产物门禁通过。
- 5174 独立浏览器检查四类文件切换、标准运行/暂停/停止、参数编辑与撤销/重做、理想高级设置展开收起、绝热膨胀 Demo 暂停/继续/终止及 Guide 入口、活塞 Guide/Free 三高度计划。删除的氢气提示框未恢复，浏览器错误为 0。
- 复用已由领域流程生成的绝热膨胀完成夹具，在界面逐步输入计算结果，再验证资料结果窗整体关闭、撤销、重做及结果保留；本检查点没有重新声称完成一次新的物理采集。
- 无并发检查任务的标准运行抽测：同 1280×720、DPR 1、464×332 画布，2–8 秒帧间隔，基线 P95 50 ms、最大 58.4 ms、超过 50 ms 为 10 帧；本检查点 P95 41.7 ms、最大 158.3 ms、超过 50 ms 为 4 帧。布局时间 0.780→0.773 秒，脚本时间 3.200→3.047 秒。单次抽测不证明消除卡顿，最终仍需多次串行复测。

接下来连续迁移完整活塞控制器、分实验视图与区域组件、文件/重命名/参数/布局动作、启动与保存恢复，再收拢绝热膨胀运行时及外壳组合。各组可以内部验证和本地提交，整体完成后才向用户交付。版本保持 6.4.0；未上传、未发布。

### 4.14 ARCH-003 整体拆分：内部检查点 2（2026-09-06）

以下是内部检查点的当时状态，最终整体完成见第 4.15 节。本检查点延续用户要求，尚未到整体交付断点。基于 `1ae473c` 本地继续拆分，未推送、未发布，版本保持 6.4.0。

- 活塞控制器拆成 14 个有界实现模块，覆盖通道、反馈、教学时钟、课程、清单、处理视图、模式/参数/采集/自由动作；单一 Hook 组合，沿用原 refs。76 个迁出命名函数的执行体 token 对照一致。显示值逐步计算、严格相等和主周期规则未改。
- 迁出 47 个受控视图组件，保留原 DOM、列表 key、错误边界和可选覆盖层的 null 语义。绝热预览和其运行时接线留到下一组，避免建立巨型 props 对象。
- 文件创建/关闭/重开/删除/选择、重命名、标准/理想参数、布局滚动动作有独立模块；标准/理想运行时注册、帧循环和运行控制继续使用原引擎 refs。78 个迁出函数保留原逻辑，输入聚焦仅通过原 0 ms 延迟端口适配。
- 初始工作区、文件集合提交、保存调度器及投影效应迁出；实时帧不新增语义保存。设置偏好、更新器、辅助窗口、导出环境与导出动作分别有自己的状态/资源所有者。
- 主入口由 21,880 行降到 12,711 行；仍有绝热运行时、教程、工作区恢复/退出和展示状态的整体拆分工作，不能据行数宣布完成。
- 严格 TypeScript（noEmit、noUnusedLocals、noUnusedParameters）通过；全量 **321 个测试文件通过**；生产构建 **2390 modules / 11 JavaScript chunks** 通过；差异空白检查通过。新增直接测试覆盖真实引擎/受控时钟、提交顺序、迟到回调、设置与辅助窗口、导出和组件交互；旧源码合同逐项指向实际所有者。
- 固定 5174 新浏览器上下文通过：四类文件切换，标准参数 15→17、撤销15/重做17、开始/暂停/停止；理想高级设置展开/收起；绝热 Demo 暂停/继续/终止与 Guide；活塞 Guide 与三组80/70/60 Free；截图要求移除的说明仍不存在；主题/语言刷新保留；关于及说明窗口。运行错误为0。
- 已完成绝热引导夹具通过界面逐步计算（115.0、31.7→107.050、102.885→1.391→0.627）；关闭资料与结果窗口的撤销/重做正确，模式保持 Guide，错误为0。夹具验收不冒充全新物理采集。
- 独立性能单次抽查：442帧、p95 25.1 ms、最大199.9 ms、超过50 ms共2帧，canvas 464×332；LayoutCount328、LayoutDuration0.88818 s、ScriptDuration3.202229 s、TaskDuration5.664679 s。测量期间无并发类型/测试/构建；仍有偶发长帧，整体交付前需要多轮同条件基线对照。

下一组继续绝热参数/反馈/教学/仪器/演示/模式切换/场景恢复、教程流程和工作区协调，直至主入口只组合有界控制器与页面区域。每组内部可自行验证，本次交付仍设在整体 ARCH-003 拆分完成处。

### 4.15 ARCH-003 整体拆分完成（2026-09-06—07）

用户要求的整体交付边界已收拢：本次从 `903e603` 的 24,984 行入口，经 `1ae473c`、`44b98de` 两个本地内部检查点，迁出业务、状态资源、运行时与生命周期职责；最终主入口为 **2,670 行**，保留显式控制器接线、页面区域装配与轻量只读投影。不是仅按行数判断完成，也没有另建巨型通用上下文。完整所有者与依赖方向见 [Workbench 界面组合合同](architecture/workbench-ui-composition.md)。

**最终批次与独立审查**

- 绝热膨胀按参数/反馈/清单/教学/仪器/自由工作区/模式/场景恢复/Demo/计时与恢复资源拆分；控制器通过七组明确输入及 view、bindings、commands、lifecycle 输出接线，不替换原领域、模式存储或运行时权威。
- 教程按状态、档案提交、覆盖层、普通工作区缓存、所有权、激活、交接/失败重试与进度分开。跨窗口租约、迟到回调、保存失败、恢复去重及取消仍走原协议。
- 工作区集合、历史、参数交互、布局、控制台、运行时资源、保存调度/生命周期与桌面退出拥有独立模块。刷新 DOM 数据仍在捕获时读取，不在构造器或渲染时提前冻结。
- 最终批次 106 个新增生产模块全部从主入口可达，无孤立模块。静态运行时依赖无环；领域未反向依赖 UI，其他模块不反向导入主入口。入口无裸 useState/useRef/useEffect/useLayoutEffect。
- 独立展开核对 99 个 effect（93 个 passive、6 个 layout）在各自 React 队列中的相对顺序不变。绝热 33 个原 effect 的回调与依赖 token 不变，按九个原调用阶段注册；修正迁移中一度提前注册的问题后再验收。
- 刷新会话 67 个 UI 字段的名字、顺序和值零差异，其中 40 个普通展示字段独立捕获。迁出前 236 个较长命名函数中 227 个函数体 token 完全一致，其余为入口装配、明确依赖端口及捕获调用替换，逐项审查。修复一个迁移时遗漏的教学计时常量导入后重新载入；后续浏览器错误为 0。
- 相对 `903e603`，`src/domain` 与 `src/features/pistonOscillation` 无改动；物理步进、采样、存档版本、历史容量及计算判定未改。

**完整验收暴露并修复的两处既有保存缺陷**

- 在 Free 26℃、Demo 运行一段时间、暂停/继续/终止、返回 Free、切文件与刷新、再进入 Guide 的连续流程中，V3 将绝热文件隔离，诊断为 `persistence-v3-heat-mode-session-not-canonical`。原始数据保留在 opaque 区，不能将恢复列表缺项解释成正常分库。
- 同一较长操作链在拆分前 `903e603` 的旧主入口也复现；即时终止 Demo 的短检查未触发。确定性最小复现不需要 Demo，关键是非默认参数下的 Explore 刷新启动。第一处根因是 `workbenchHeatCapacitySessionRestore.ts` 漏认合法的显式 null（Explore），真实 bootstrap 将其错当 Free，随后打开归一化把 Explore 初始公共读数重拍进保留的 Free 快照，造成25℃公共读数与26℃权威域混合。
- 第一处最小修复仅将显式 null 纳入合法模式；undefined、缺省和未知模式仍使用原 fallback。没有放宽 canonical 校验，没有修改 V3 格式、领域物理或活塞答案判定。
- 在现有 `heatCapacityExploreMode.test.ts` 补完整 V3 decode→真实 session bootstrap→打开归一化→再次保存/恢复→切 Guide 路径；确认 Free 快照、26℃和3次计划保留，且仍可正常投影。该测试先在 null 被错误变成 free 的断言处失败，再经单分支修复通过。

- 第一处修复后继续真实浏览器复放，Free 快照已合法，但活动 Guide 会话仍被隔离。第二处根因是 Guide 物理状态已按自己的配置初始化，环境温压及容器读数却从此前 Free 文件继承；只补齐四个派生公共字段，使它们与原 Guide 配置一致，不改配置或物理规则。
- 第二处回归覆盖非默认 102.3 kPa、26℃从 Free 分别进入 Demo/Guide，经真实 materializePersistenceV3Snapshot→生产代际提交→恢复。修复前 Demo 合法、Guide 被隔离；修复后两者合法，完整 Free 会话深比较不变，恢复仍为 102.3 kPa、26℃和3次计划。两处修复共修改3个已有文件（2个生产模块和1个测试文件），未新增格式或修复性迁移。
- 最终全新浏览器上下文复放：Free设置102.3 kPa/26℃，Demo至步骤9再暂停/继续/终止，返回Free，文件切换并刷新，再进Free与Guide。最新生产存储四份文件完整、opaque为空；随后重新观看产品介绍、启动教程并刷新，普通存储仍四份、opaque为空，保存的Free仍为102.3 kPa、299.15 K及3次计划。浏览器错误为0。

**全新活塞三组实际验收**

在独立 5174 浏览器上下文中新建 `Piston Oscillation - 002`，通过真实鼠标、键盘及仪器开关完成三组采集，没有注入实验样本或答案。每组 501 个样本、0.500 秒；按压不足时提示继续下压且没有保存无效测量。分别设置 80/70/60 mm，框选结果如下：

| 高度 mm | 显示 t₁ / t₂ s | 主周期数 | 显示 T s | 显示 T² s² |
| --- | --- | --- | --- | --- |
| 80 | 0.034 / 0.221 | 5 | 0.03740 | 0.0013988 |
| 70 | 0.032 / 0.102 | 2 | 0.03500 | 0.0012250 |
| 60 | 0.030 / 0.095 | 2 | 0.03250 | 0.0010562 |

- 不足两个周期的窄框选被明确拒绝。第一组界面显示 11 个主极值、5 个周期；原始显示曲线上 0.169/0.171 s 与 0.186/0.188 s 等额外小起伏没有增加周期计数。事后只读核对实际样本和选点结果，与界面一致。
- 三组完成后真实关闭电源，正常进入离线拟合和计算。只用表中显示值独立重算：斜率 **58.373 m/s²**、截距 **−0.0016042 m**、R² **0.99993**。
- 界面给定 m=0.0485 kg、d=32.5 mm、P=1.01×10⁵ Pa；按显示且已舍入的斜率逐步算得 **A=0.0008296 m²、γ=1.334、相对误差=4.71%**，后台参考值完全一致。
- 特意提交精度合规的近值 **T=0.03739、A=0.0008295、γ=1.333、误差=4.70**，四项均判数值错误。选择继续作答并输入精确值后通过，没有用“查看并继续”绕过判定。
- 完成并退出后切换文件再刷新，完成状态、三组原始样本、选点、结果与正确/错误尝试记录的字段值均一致；序列化字段排列不同不等于数据改变。后续进入教程后，已完成活塞文件仍保留在普通工作区存储中。

**最终验证与限制**

- 严格 TypeScript（`--noEmit --noUnusedLocals --noUnusedParameters`）通过，全量 **341 个测试文件通过**，生产构建 **2494 modules / 11 JavaScript chunks** 通过。相关测试直接覆盖计时资源、晚到回调、文件/历史/保存顺序、教程所有权/交接和只读模型。旧源码合同逐项定位实际所有者，保留主入口接线检查，没有拼接整个目录模拟旧文件。
- 固定 5174 浏览器：标准参数 15→17、撤销15/重做17、开始/暂停/停止；理想高级设置展开/收起及 P-V/P-T；绝热 Demo 暂停/继续/终止、Guide、Free 参数 25→26、跨模式/文件/刷新后重新进入 Free 仍为26；四类文件切换与模型加载；主题/语言刷新保留。已删除的活塞说明框未恢复。
- 完成态绝热领域夹具通过真实界面计算 115.0、31.7→107.050、102.885→1.391→0.627，并验证结果窗关闭、撤销、重做，模式仍为 Guide。该夹具只验证结果与历史流程，不冒充本批全新绝热物理采集。
- 教程经真实设置界面重新观看产品介绍、选择学习需求、确认开始后进入临时教学文件；第二窗口正确提示已在另一窗口运行，刷新原窗口正确续接当前演示学习阶段。异步交接、失败重试及退出保护由直接测试覆盖。
- 六轮标准画布性能对照使用同一台机器、同一当前依赖环境及同一四文件起始存储，每轮独立浏览器上下文，1280×720、画布464×332、前台可见，串行交替测量，期间不并发类型检查、测试或构建。每轮运行8秒，帧间隔统计第2–8秒，CDP耗时统计整个运行段。对照通过浏览器响应替换载入903e603旧主入口；不是完整旧提交检出。
- 两侧测试响应均只移除本地源码导入的HMR时间戳，以避免旧入口与当前依赖的时间戳不同导致保存单例重复。调整探针前的启动失败没有进入性能结果；正式六轮均加载四文件成功，浏览器错误为0。

| 对照轮次 | 有效帧数 | 帧间隔P95 ms | 最大间隔 ms | 超过50 ms次数 | 布局次数 | 布局耗时 s | 脚本耗时 s | 任务耗时 s |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 旧入口1 | 643 | 16.6 | 100.0 | 2 | 419 | 0.841822 | 2.781475 | 5.036982 |
| 最终1 | 691 | 8.5 | 25.0 | 0 | 456 | 0.789560 | 2.908210 | 5.091013 |
| 旧入口2 | 693 | 8.5 | 91.6 | 2 | 448 | 0.888581 | 2.994745 | 5.386821 |
| 最终2 | 710 | 8.5 | 16.7 | 0 | 455 | 0.837710 | 3.099724 | 5.409628 |
| 旧入口3 | 621 | 16.7 | 91.6 | 4 | 359 | 0.789295 | 2.878251 | 4.896450 |
| 最终3 | 660 | 16.6 | 58.4 | 1 | 429 | 0.955126 | 3.288255 | 5.794137 |

- 三轮中位数：P95由16.6降至8.5 ms，布局耗时0.841822→0.837710 s；超过50 ms的帧合计8→1。脚本耗时中位数2.878251→3.099724 s（约+7.7%），任务耗时5.036982→5.409628 s（约+7.4%）。本轮未观察到此前普遍点击与动画卡顿，但不能据此声称所有性能指标改善或长帧归零；该短时样本仍有58.4 ms长帧，后续真实长时使用应继续监视。
- 验收使用的独立浏览器上下文已关闭；本批临时保存的两份隔离诊断JSON已清理，没有删除既存输出。固定5174预览继续运行，未操作用户浏览器中的现有实验文件。
- 保持 6.4.0，未改依赖、未制作桌面安装包、未推送或发布；浏览器不具备桌面科学 PDF/本地导出桥接，桌面退出/导出边界由现有直接测试覆盖，未声称完成桌面安装包实机验收。旧教学物理候选参数和偶发长帧仍是后续证据边界，不能把架构验收当作物理标定。

### 4.16 PO-CAL-001 模型扫描与评价口径完成（2026-09-14）

本批完成独立的模型证据阶段。新增 `scripts/analysis/pistonOperationSweepModel.ts`、`runPistonOperationSweep.ts` 及直接行为测试，复用当前虚拟手受力、热过程、连续传感器观测、主周期识别和显示值精确计算链。没有修改 `src/`、包版本、依赖清单、用户文件、持久化或评分。

- 固定扫描为 Real 空气、80/70/60/50/40/30 mm，6 种参考行程 × 4 种下压时间 × 3 种停留 × 5 个传感器种子，共 432 个物理轨迹、2,160 个观测实例。
- 选区预先固定为最早被现行算法接受的两个完整主周期，不依据 γ 反向调整。未触发或缺少选区的案例保留；三高度、六高度必须完整才拟合。
- 产出 [扫描口径](instrument-modeling/piston-oscillation/validation/operation-sweep-protocol.md)、[扫描报告](instrument-modeling/piston-oscillation/validation/operation-sweep-v1/report.md)、全量逐例指标、720 次完整集合拟合尝试、12 条代表性正式记录和 19 个来源文件指纹。证据含原始观测、操作轨迹及物理/传感器快照，可独立复现。
- 全部实例中 1,706 个可处理两周期、454 个未发生下降触发；触发后未找到两周期选区的实例为 0。三/六高度各有 240/360 次完整拟合，失败分母没有删除。
- 当前 200 参考像素、1.1 s 下压时，同一动作从 80 到 30 mm 的释放真实压强约为 120.44–139.48 kPa，说明统一手势不对应统一释放压强。真实压强只作诊断，不进入学生答案。
- 固定两个周期时，当前行程下三高度显示 γ 为 1.458–1.632、误差 4.14–16.60%；六高度为 1.445–1.456、误差 3.21–4.00%。0.4 s 停留仍出现相同范围。可处理门禁不保证结果精度；1 ms 端点采样及三点拟合对选点敏感，不能据此恢复容差、增设评分惩罚或凭最小 γ 误差调参。
- 没有轨迹采样点低于当前 0 mm 下限或超出模型 20–200 kPa 范围。360 个实例反弹越过 80 mm 名义设置参考线，最高约 90.15 mm；80 mm 是平衡高度设置限制，不是已测得的碰撞上限。实物上侧净空仍未知，未据此声称发生机械碰撞或确定安全操作范围。
- 四组已有实测 CSV 的 SHA-256 完全不变；其实际高度为 50/40/30/20 mm，缺少完整下压/停留/同步释放及重复条件。本报告没有将这些数据冒充本次网格的实物验证。

验证与交付边界：

- 严格 TypeScript（`--noEmit --noUnusedLocals --noUnusedParameters`）通过；4 个直接相关测试文件和全量 342 个测试文件通过；生产构建为 2494 modules / 11 JavaScript chunks，输出门禁通过。
- 新测试覆盖确定性种子、连续观测状态、正式记录归一化、失败保留、全高度拟合和显示值计算；`0.03250² → 0.0010562` 的十进制中点舍入仍正确，选点不使用隐藏物理参数。完整扫描后再次运行 `node scripts/analysis/runPistonOperationSweep.ts --check`，2,160 个实例及所有证据逐项一致。
- 5174 独立浏览器回归通过四类文件切换、标准参数 15→16 及开始/暂停/停止、理想高级设置展开/收起和 0.80 预设、绝热 Demo 暂停/继续/终止及三次自由实验组设置、活塞 Guide/Free 切换和 80/70/60 mm 计划。切换氦气后已删除的说明框未恢复；刷新后标准参数 16、活塞自由计划及氦气选择保留。页面和控制台错误为 0，独立上下文已关闭，用户现有浏览器文件未操作。未重复本批无关的完整三组手动物理采集，也未做新的帧耗时对照。
- 最新 `npm.cmd audit --omit=dev` 返回失败：1 项高危 `js-yaml 4.3.1` 公告，详见第 5.4 节。未静默升级依赖，不能宣称全部发布门禁通过；未制作 NSIS 安装包、上传或发布。

本批在可复现证据完成处本地保存。PO-CAL-001 的模型部分已完成，科学操作标准仍待同步实测交叉验证；PO-CAL-004 的阈值和评分没有冻结。当时提出优先处理的 `SEC-001` 已在后续第 4.17 节完成；物理批次仍以补齐真实证据或独立定义绝热终点判据对照为边界，不再自动续做 ARCH-003。

### 4.17 SEC-001 更新清单依赖修复（2026-09-14）

用户明确同意从官方 npm 下载补丁，并使用已确认的 D 盘安装、缓存和临时目录。安装禁用生命周期脚本，`package.json` 与锁文件中的直接约束更新为 `^4.3.2`，实际安装 `js-yaml 4.3.2`。`electron-updater 6.8.9` 的生产解析路径和构建工具均解析到该补丁版本；锁文件中其他依赖版本不变。

- 新增 `tests/workbench/workbenchUpdaterYaml.test.ts`，直接调用已安装 `electron-updater` 的 `parseUpdateInfo`、`resolveFiles` 和本项目更新说明解析入口。使用内存清单验证当前发布说明、中文、日期、结构化章节、安装包路径、校验值和大小；兼容旧 `path + sha512` 清单，拒绝空清单、损坏 YAML 和缺少校验值的文件。
- 安全回归先在 4.3.1 上确认空映射合并没有消耗预算，再在 4.3.2 上通过：微型显式预算用例和实际 updater 默认入口均拒绝超预算输入；另外覆盖过长合并序列，并验证拒绝后仍可读取正常清单。默认预算用例只含 10,100 次空映射访问，未运行压力攻击或下载真实安装包。
- 安装器报告同步 10 个本地包；逐项比较锁文件，依赖版本变更只有 `js-yaml`，已安装版本均与锁文件一致。生成型法律清单保持 467 条记录，更新 YAML 版本及生成时间；许可正文同时忠实同步本地包中的 3 处版权年份／联系地址变化，没有手工改写许可。
- 首次全量检查发现研究报告证据仍保存旧 `package.json` 指纹。通过原生成器重新运行后，所有实验 CSV、参数和数值汇总不变，仅更新两份 JSON 中的生成信息、提交来源及相应 SHA-256；保留原指纹校验测试。

验证与交付边界：

- 8 个更新链及法律清单相关测试通过；新增解析测试已验证修复前失败、修复后通过。最终严格 TypeScript（`--noEmit --noUnusedLocals --noUnusedParameters`）和全量 343 个测试文件通过；生产构建为 2494 modules / 11 JavaScript chunks，输出门禁通过。`legal:check` 确认 467 条记录有效；构建后再次运行生产审计，结果为 0 项漏洞。
- 5174 独立浏览器回归通过四类文件切换、标准开始／暂停／停止与边长 15→16、理想高级设置展开／收起及 0.80 预设、绝热演示暂停／继续／终止、活塞 Guide／Free 切换和 80／70／60 mm 计划。确认氦气选择生效后刷新，气体选择和计划保留；标准参数及理想预设也正常恢复，已删除的氦气说明框未恢复。
- 关于窗口仍显示 6.4.0，网页点击检查更新后提示“仅桌面安装版可用”；第三方清单页面与许可正文均显示 `js-yaml 4.3.2`。页面和控制台错误为 0，独立上下文已关闭；用户已有浏览器文件未操作。本批没有做新的帧耗时比较或完整三组手动采集。
- `src/`、`electron/`、物理参数、精确判题和持久化实现均未修改，应用及锁文件版本保持 6.4.0。未制作 NSIS 安装包，未测试已安装桌面客户端联网下载与替换，未推送或发布；本批验收范围是依赖、真实更新解析入口及现有更新状态／失败处理回归。

SEC-001 在依赖修复与回归验证处收口，不自动扩展到发布或新物理机制。后续按阶段 E 补齐真实标定证据，或先定义 HC-PHYS-001 的独立比较口径。

### 4.18 HC-PHYS-001 终点判据模型对照（2026-09-15）

用户要求进入下一阶段。开始前确认架构与依赖批次已结束，真实活塞标定仍缺同步实测；先报告终点判据的现有证据和范围，再完成独立分析工具，没有改动应用生产源码。

- 新增 `scripts/analysis/heatCapacityEndpointComparisonModel.ts`，复用当前放气状态机、有限时长打气、自由物理、传感器、声音反馈和正式记录计算。三种策略分别为程序声音停止代理、新样本显示恰为 0.0 mV、主通路开放后固定 0.6 s；无容差、无默认终点补救，也不使用理论 γ 反向选时刻。
- Real 空气，标准 18 次打气、低压 4 次打气、流量系数 0.4 三个场景，各五个种子、三个反应延迟，共 135 例；同一准备快照分支。计时保留 0.42 s 开启、命令立即断流、0.42 s 关闭和关闭动画后 300 s 回温。初态是明确的已预热/调零夹具，不冒充完整用户操作。
- 全部 135 例另做 0.02 s 配对，基准为 0.04 s。两种网格均为 129/135 例有效、6 例观察 5 s 内未触发；但 12 对状态改变，全部来自显示归零。标准无延迟基准中，声音代理在 0.32 s、显示归零在 1.28 s、固定策略在 0.60 s；显示归零的最大配对终点差达 2.82 s。网格变化同时影响积分、采样和噪声，不能声称积分器收敛或真实操作可靠性。
- 新增固定协议与 `endpoint-comparison-v1/` 证据：全部逐例 CSV、完整聚合和配对、准备快照、代表/失败轨迹及来源指纹。`node scripts/analysis/runHeatCapacityEndpointComparison.ts --check` 只读重算核对全部输出，已通过。
- 新测试覆盖陈旧/初始零值、邻近非零值、暂停/关闭引起的静音、首个信号锁存、同快照无突变、动画与反应计时、观察窗口内未触发及反应超窗、缺值保留、全部记录的独立显示值重算与全部步长配对。没有给产品增加容差或新的错误惩罚。

验证与交付边界：

- 严格 TypeScript（`--noEmit --noUnusedLocals --noUnusedParameters`）、新增终点测试与全量 344 个测试文件通过；生产构建为 2494 modules / 11 JavaScript chunks，图标、467 条法律记录和输出门禁通过。2026-09-15 生产依赖审计为零漏洞，证据只读重算通过。
- 5174 独立浏览器通过四类文件切换、标准开始/暂停/停止、边长 15→16、理想 0.80 预设及高级设置展开/收起、绝热演示暂停/继续/终止、活塞引导退出与自由 80/70/60 mm 计划。确认氦气选择生效后刷新，气体与计划保留；标准边长和理想预设也正常恢复，已删除的氦气说明框没有恢复。结果窗口打开、撤销、重做和切换数据表通过；页面及控制台错误为 0。
- 活塞模型加载与参数布局已截图查看；本次临时截图已清理，独立上下文已关闭，用户浏览器文件未操作。本批没有新做帧耗时量化比较、完整三组手动采集或桌面 PDF 导出；已有对应自动测试通过，应用运行路径未变。
- 声音策略仅是程序反馈代理，不含真实音频输出和人耳判断；全部数据为教学模型证据。本批不决定界面交互，不改物理参数、演示时长、评分、状态权威、持久化或撤销；不制作安装包、不改版本、不推送或发布。

HC-PHYS-001 的协议与模型比较在此收口。后续进入教学界面前需明确代理含义、失败展示与网格限制；真实科学标准继续等待同步动作、声音、压力显示及回温读数。不要重复扫描，也不自动重开 ARCH-003。

### 4.19 发布前本地候选与实际桌面验收（2026-09-15，已完成）

用户要求继续到发布前，验收后再决定是否发布。本批按现有功能收口，最终候选为 `output/local-acceptance/2026-09-15-candidate-02/`，从其中的 `启动验收.cmd` 使用独立 `user-profile/`。详细步骤、验收范围、性能和限制见[本地验收说明](releases/local-acceptance-2026-09-15.md)。

- 增加 `HSL_RELEASE_OUTPUT_DIR`，将构建、更新描述生成、三项资产校验统一到全新的本地候选目录；拒绝已有目录、越界和链接跳转。标准 `release/` 入口保留，原 6.4.0 三项资产哈希未变，构建始终 `--publish never`。
- 桌面启动支持显式绝对 `--user-data-dir`，普通启动仍沿用原路径。实际打包程序完成首次设置、新窗口创建与关闭、正常退出保存；重启后标准边长 16、活塞氦气与 80/70/60 mm 计划恢复正确。主动关闭的额外窗口按原合同从注册表移除，没有把它当成重启后应自动恢复的窗口。
- 发现并修复理想 P–T / P–N PDF 孤立结论页；单图预留结论空间、节标题与正文相邻，补充真实 ReportLab 分页测试。更新随附导出器指纹和生成型许可清单。Windows 安装包测试夹具曾出现一次 `EBUSY`，仅为该测试的文件写入增加最多 1 秒的限定重试，未放宽断言。
- 最终严格 TypeScript、346 个测试文件、生产依赖审计（0 漏洞）、2494 modules / 11 块的生产构建、完整 NSIS 安装包构建和三项资产门禁通过。11 个打包 Electron 源文件与工作区逐字节一致。包和锁文件均保持 6.4.0。
- 打包导出器生成 8 类合成报告共 24 页并渲染检查；活塞及绝热共 17 页与已检查图逐像素一致。真实 Windows 保存对话框完成活塞六次报告及最终包 P–T 报告，均使用随附导出器，后者为完整一页。合成样本不替代实测或完整三组人工采集。
- 5174 四类文件、参数、演示/引导/自由模式、刷新恢复、结果标签和撤销重做通过，页面错误 0。活塞氦气说明框未恢复，精确判题和物理运行路径未修改。
- 性能独立 8 秒抽测、统计第 2–8 秒：开发预览标准 P95 91.7 ms、最大 166.6 ms、34 帧超过 50 ms；绝热演示 P95 33.1 ms、最大 341.6 ms、13 帧超过 50 ms。最终桌面标准 P95 8.4 ms、最大 25 ms、0 帧超过 50 ms。保留开发预览长帧，不将不同环境当作优化前后对照，也不宣布全部卡顿消失。

本轮未执行安装到日常环境、卸载、在线更新或发布；候选沿用未签名的本地构建。接下来等待用户验收反馈。正式发布前还需用户决定版本号、更新发布说明、重新核验远端角色和发行检查；不得直接发布这个同版本候选。物理实测和终点教学界面仍遵守阶段 E 的证据与范围边界。

### 4.20 6.4.1 发行基线与三仓库发布约定（2026-09-15）

用户在本地候选验收阶段之后明确授权制作并发布 `6.4.1`，且指定先更新“杠一”私有仓库，再更新匿名源码历史，最后发布公开安装包。此次授权替代第 4.19 节当时的等待验收停点及中途提出的 6.5.0 版本号；不扩大架构或实验功能范围。

- 同步包与锁文件版本、三语言 README、结构化发布说明、构建告知和生成型许可清单；既有版本说明完整保留。重建导出器清单的产物哈希，导出器源码指纹不变。
- 版本变更使通用验证记录中的包清单指纹失效，已通过原证据生成入口刷新。科学数据表未变化，结果摘要除生成时间外逐项相同；未放宽验证门禁或重新调整物理模型。
- 严格 TypeScript（含未使用符号）、全量 346 个测试文件、生产构建（2494 modules / 11 个 JavaScript 分块）、生产依赖审计（0 漏洞）、完整 NSIS 构建及三项更新资产门禁通过。最终包内 11 个 Electron 文件、运行依赖清单和结构化版本说明与源码一致。
- `6.4.0 → 6.4.1` 使用真实安装包和 blockmap 的增量计划重建通过，复制 63.28%、下载 36.72%，重建结果与新安装包逐字节一致。
- 5174 与实际桌面包均显示 6.4.1。四类文件切换、标准启停和参数、理想气体高级设置、绝热演示暂停/继续/终止、活塞 Guide/Free 及氦气计划、结果标签撤销重做和刷新恢复通过，网页错误为 0。独立桌面资料目录恢复旧参数 16，改为 17 后正常关闭并重启仍为 17。
- 随附导出器实际自检通过；使用最终随附程序输出 P–T 报告，单页 A4 的图表、数据和结论完整，已经渲染检查。未在用户日常安装目录执行升级或卸载。
- 发布必须包含同版 EXE、EXE.blockmap 和 latest.yml，下载文件名、SHA-512、大小及更新仓库保持一致。标签、说明、校验值和证据边界见 [6.4.1 发行记录](releases/v6.4.1.md)。外部发布状态以各仓库实际引用和 GitHub Release 为准。

此次没有重新采集整套三组活塞或扩大物理标定；保留第 4.6、4.15 节实操证据和现行精确计算测试。旧拟合及下游计算可能需要重做；原始数据和有效周期保留。开发预览偶发长帧及模型缺少同步实物标定的边界继续有效。

**发布回执与当前停点**

- 私有发行源码提交为 `1c022bd`，对应匿名源码为 `2845cb9`；公开发行文档提交为 `1653778`。三处均有各自的 `v6.4.1` 注释标签。源码仓库后续的发行回执文档提交不移动这些标签。
- GitHub Release 已于 2026-09-15 10:57（Asia/Shanghai）正式公开并设为最新稳定版；三项上传资产的大小和 GitHub SHA-256 均与本地一致。
- 35 个新增源码提交保留原作者／提交者日期，匿名作者及提交者均为 `Project Team`；沿用 114 条研究工作材料排除规则，1,093 个程序与测试文件同私有源码一致。未推送旧 `private` 目标。
- 使用独立资料目录启动 6.4.0 桌面候选，真实点击检查更新。首次连接中断后重试，成功发现 6.4.1，中文摘要、五组详细说明及手动下载地址正确。没有下载或安装更新到用户本机。
- 补充的干净 Windows 工作流已完成公开安装包下载、6.4.0 安装、工作区保存和旧版重启恢复；随后 `npm ci` 因锁文件缺少 `@emnapi/runtime@1.11.3` 记录失败，新版覆盖安装步骤未执行。该项不能标记为升级验收通过；现有本地增量重建结果仍有效。
- 缺项涉及构建工具的可选 WASM 开发依赖；当时本地生产及全依赖审计均为 0 漏洞，并在下载缺项前停下征求确认。用户随后授权的补丁修复见第 4.21 节；保留原失败结论，不移动既有标签或替换已发布资产。

### 4.21 BUILD-001 锁文件修复与 6.4.2 补丁（2026-09-15）

用户同意从官方 npm 获取必要构建依赖，继续使用 D 盘临时目录和缓存，并要求检查新增开发记录、完成后制作补丁版本。本批从干净的本地交接提交 `286a4ee` 开始，目标为 `6.4.2`。

- 复现 CI 的 npm 11.19.0 校验失败：`@napi-rs/wasm-runtime` 必需的 peer `@emnapi/runtime@1.11.3` 未记录在锁文件中。本机 npm 11.6.2 未检出；使用隔离的同版工具复现后，只补这一条记录，保留原有依赖版本、下载地址和完整性哈希。
- 隔离目录实际执行全新安装成功；新增回归遍历锁文件中的 42 个必需 peer，覆盖未安装到 Windows 本机的可选平台分支。CI 将锁文件安装门禁前移到旧版安装之前，避免再次在变更测试环境后才发现依赖缺项。
- 审查自 6.4.0 以来新增或修改的当前开发文档，修正已过期的未发布状态、架构索引遗漏和后续入口；保留归档及模型扫描当时的包版本、数值、指纹和证据边界。应用版本升级不改变持久化 schema 版本。
- 同步 6.4.2 包版本、三语言 README、结构化发布说明和构建告知，保留旧版说明及 6.4.1 标签和资产。`src/`、`electron/`、实验机制、精确判题、状态权威和保存格式不变。

最终检查、Windows 升级实证及发布回执统一记录在 [6.4.2 发行记录](releases/v6.4.2.md)。发布仍按私有源码、匿名源码历史、公开安装更新资产的顺序执行；未完成的门禁不可提前标记通过。

## 5. 已知风险与仍需留意的问题

### 5.1 性能回归监视

最新发布前抽测见第 4.19 节：实际桌面包短时样本较平稳，但 5174 开发预览仍测到长帧。用户应重点验收连续操作与动画；后续若处理性能问题，应重新隔离环境和根因，不凭开发/生产两种环境的差异直接调整实验步进或恢复已拆分结构。

此前曾出现整个界面明显卡顿：文件切换、模型加载、模式切换、参数调整和点击都有延迟，动画会停顿后跳变。该问题后来已修复且用户确认恢复正常，但后续拆分工作必须把它视为高风险回归项。

每个架构批次至少人工检查：

- 新建和切换实验文件；
- 首次加载三维模型；
- 演示/引导/自由模式切换；
- 展开参数、改变参数与完整重置；
- 点击反馈与主要动画是否立即响应；
- 浏览器控制台是否出现错误、警告或连续重复日志。

### 5.2 尚待真实证据的物理参数

活塞实验的按压幅度、释放压强、下压时间、最低点停留、热损耗、机械阻尼和传感器动态层仍含教学候选值。这些值不能仅凭“算出的 γ 接近理论值”就宣布完成标定。详细条目见 `docs/ideas/improvement-ideas.md` 的 `PO-CAL-001` 至 `PO-CAL-004`。

### 5.3 不能误当成当前任务的旧资料

- `docs/backlog/piston-oscillation-gas-and-scheme-design-draft.md` 已完成，只用于追溯已确认决策。
- 旧研究报告大纲、历史开发路线图和归档复选框不作为当前计划。
- 若继续研究报告，按当前代码和证据重新建大纲，不在 2026 年 7 月旧稿上继续补丁式修订。

### 5.4 生产依赖审计风险已修复

2026-09-14 修改依赖前，实际运行 `npm.cmd audit --omit=dev` 检出 1 项高危拒绝服务漏洞：[GHSA-2883-xcg3-v3hh](https://github.com/advisories/GHSA-2883-xcg3-v3hh)。`electron-updater 6.8.9` 当时包含 `js-yaml 4.3.1`。第 4.17 节已将实际依赖更新为公告修复版 4.3.2，并通过真实更新清单入口验证合并工作量限制；当前生产审计为零漏洞。

`SEC-001` 不再是待办。零漏洞结论只对应本次生产依赖审计，不替代未来发布前的新审计、安装包构建或已安装客户端更新验收。

## 6. 推荐的后续优化顺序与大断点

### 阶段 A：新对话恢复与全局基线

这是任何新一轮改动前的必做阶段：

1. 阅读第 1 节列出的文件。
2. 检查当前分支、工作区状态、最近提交、版本号和三个源码远端，不得重置用户改动。
3. 在固定地址 `http://127.0.0.1:5174/` 启动或复用预览。
4. 运行严格 TypeScript 检查、全量测试和生产构建；发现基线故障时先报告并定位，不把它混入新功能。
5. 只要不存在需要用户决策的差异，可直接进入本文件标明的未完成阶段，不必重复已经验收的阶段 B。

### 阶段 B：活塞引导和精确计算链的最终人工验收

本阶段已于 2026-09-05 完成，验收数值、缺陷修复及限制见第 4.6 节。以下保留为后续修改精确计算链时的回归清单。

使用全新的活塞振荡法文件，不依赖旧测试文件：

1. 完整完成三组引导实验，例如 80、70、60 mm。
2. 每组框选不少于 2 个主周期，确认局部小峰不会被多算，含糊区段会被明确拒绝。
3. 确认最后关闭电源后立即进入数据处理与计算，而不是再次要求设置参数或关闭电源。
4. 完成三组拟合，逐项记录界面显示的 `T²`、`h`、`m`、`d`、`P`、斜率、`A`、`γ` 和相对误差。
5. 独立按界面已经显示和舍入的数据重算一遍，确认后台未使用隐藏精度。
6. 专门验证“数值接近但不相等”会判错，规定精度下完全一致才判对。
7. 验证完成状态、回看、文件切换和重新打开后的结果一致性。

若发现缺陷，先修正本阶段并再次完成同一验收。该流程作为后续回归合同保留。本次已按整体拆分断点再次完成，数值和保存恢复证据见第 4.15 节。

### 阶段 C / D：工作台整体拆分（已完成）

ARCH-003 已按用户扩大后的整体断点完成，包含绝热膨胀模式交互、场景生命周期、保存与退出协调。当前职责边界见 [Workbench 界面组合合同](architecture/workbench-ui-composition.md)，最终验证见第 4.15 节。

主入口保留显式控制器接线、页面区域装配和轻量只读投影；不再直接持有状态、引用或注册裸 effect。仪器流程、教学计时、文件历史、保存恢复和教程所有权均有独立所有者。后续不要把“继续迁出一个小模块”当作自动恢复的待办，也不要仅为降低行数隐藏显式依赖。

### 阶段 E：物理标定与教学优化

架构已稳定，活塞模型证据见第 4.16 节，SEC-001 依赖修复见第 4.17 节，绝热终点判据对照见第 4.18 节。继续按以下边界推进：

1. `PO-CAL-001` 的指标和六高度模型扫描已完成；下一步补齐同步动作、压强与各条件重复实测，交叉验证操作范围及实物净空，再冻结科学标准。
2. `PO-CAL-004` 继续等待上述交叉验证，再校准引导触发、曲线质量阈值和评价区间。不得仅凭模型 γ 误差最小值调整门禁。
3. `PO-CAL-002`、`PO-CAL-003` 必须等待新的可追溯实测数据，不得凭当前模型自我证明。
4. `HC-PHYS-001` 的终点比较协议和配对模型证据已完成。转入教学界面前需确定程序声音代理、未触发展示及观察网格限制，不直接将模型结果冻结为真实操作标准。误差拆解和传感器实测标定仍可各自作为独立大批次。

新增物理机制、修改科学标准或转入其他实验的大批次前，仍需向用户报告可选项、现有证据、预期收益和风险。当前扫描不产生重复扫描待办，也不授权缺少实测依据的标定。

### 阶段 F：6.4.2 补丁与升级验证收口

第 4.20 节的 6.4.1 已发布，第 4.21 节补齐构建锁文件并收口开发记录。按第 9 节完成 6.4.2 的私有源码、匿名历史和公开更新资产，并以实际 Windows 升级证据确认兼容性。后续先核验远端和发行记录，不重复发布；新的功能、物理或架构批次仍需新的用户任务。

## 7. 每一批改动的统一验证门禁

按风险选择相关测试，但大断点至少执行：

1. 严格 TypeScript 检查，包括未使用符号检查。
2. 与改动直接相关的单元/集成测试。
3. 全量测试。
4. 依赖生产审计；若本批未改依赖，可说明沿用最近一次结果，但发布前必须重跑。
5. 生产构建。
6. 固定端口 `5174` 预览，禁止悄悄换端口。
7. 真实浏览器人工或自动流程验收，检查控制台、动效和响应延迟。
8. 检查保存、恢复、模式切换、撤销/重做、导出和结果窗口是否受到连带影响。
9. 检查工作区差异，只提交本批相关文件，不覆盖或清理用户的无关改动。

如果一个小阶段验证无误且没有设计决策，可以继续下一个小阶段；到达大断点、出现行为歧义、缺少真实证据或需要扩大范围时再停下来请用户确认。

## 8. 用户偏好的协作与保存方式

- 先调查当前实现和资料，再给出问题、根因、风险与修复方案；行为含义不清时先让用户决策。
- 小问题可以合并成一批；大问题分别成批。每批都有明确完成条件。
- 小阶段测试通过后可连续推进，不必频繁打断；大断点必须汇总本阶段全部新增改动和验证结果。
- 进入下一个大阶段前，先把当前改动保存成本地 Git 提交。
- “保存到本地”只表示本地提交，不表示上传 GitHub。
- 只有用户明确说“上传 GitHub”时才可推送。不要因为完成阶段、写完文档或测试通过而自动上传。
- 中间过程默认保持当前版本号；涉及重大上传或正式发布准备时，先询问用户使用什么版本号。
- 没有需要决策的内容时，主动继续做到当前阶段完成；不要为了形式上的确认反复停顿。

## 9. 私有源码、匿名公开历史与发布仓库

用户的预期工作流是：

1. 本地是最新开发权威，先在本地修改和提交。
2. 用户明确要求上传时，完整开发历史应先进入主要私有源码仓库。
3. 随后检查作者身份、个人信息、私有研究工作材料和其他不应公开的内容。
4. 将经过匿名化处理的同等源码历史更新到公开历史仓库，并尽量保留原提交日期。
5. 安装包与自动更新资产使用独立的发布仓库；公开源码历史仓库不是安装包发布仓库。

当前配置需要特别谨慎：

- `origin` 实际指向 `<private-source-repository>`，是用户此次明确选定的私有源码仓库；完整历史先更新这里的主分支、当前开发分支与版本标签。
- `private` 是另一个旧私有目标，此次明确排除，不推送、不改引用。
- `public-history` 指向匿名公开源码历史仓库。
- `package.json` 的 `repository` 指向公开历史仓库。
- 桌面自动更新发布目标是独立的 `gas-laws-lab-release` 仓库。

因此，未来任何推送前都必须先获取远端引用、比较提交关系和确认目标角色。不要下载或覆盖整个工作区来解决差异，也不要盲目执行强制推送。若远端职责仍有歧义，先把各远端的分支、可见性和差异报告给用户，再执行上传。

用户已授权上述三处的 6.4.1 更新，并随后授权按相同流程制作 6.4.2 补丁。匿名历史继续使用 `Project Team` 作者及提交者身份、保留原提交日期，沿用已有私有研究工作材料排除规则；第三方许可、公开引用和实验数值不得因匿名化而篡改。公开发行仓库只接收发行文档、标签及安装更新资产，不放开发源码。

## 10. 可直接粘贴到新对话的提示词

```text
请继续当前仓库的气律实验室开发。首先完整阅读仓库根目录 AGENTS.md，然后依次阅读 docs/current-development-handoff.md、docs/README.md、docs/ideas/improvement-ideas.md，以及与当前任务相关的活塞实验规范和架构文档。不要把 docs/archive 中的旧计划当成当前待办。

当前时间背景是 2026-09-15（Asia/Shanghai），包版本为 6.4.2，当前开发分支应为 codex/piston-oscillation-development。原功能基线为 c9bf079，后续已完成本交接第 4.6 节的全新活塞引导验收，并修复操作镜命中恢复、周期平方的十进制中点舍入及第 4.9 节的隐藏操作镜持续绘制。请从最新本地提交继续。不要恢复容差判定，也不要在后台使用用户看不到的高精度数值。旧的活塞计算测试文件可以放弃，以新版本正确行为为准。

先做只读检查：确认 Git 状态、当前提交、版本号、远端关系和固定端口 5174；不得重置或覆盖本地内容。然后在改动前运行严格 TypeScript、全量测试和生产构建，确认全局基线。活塞阶段 B 已通过并本地提交，除非发现新回归，不必重复整个已完成批次。

ARCH-003 整体拆分和 HC-UX-003 窗口历史修复已经完成，PO-CAL-001 六高度模型扫描、SEC-001 依赖修复及 HC-PHYS-001 终点判据配对证据也已完成。6.4.1 已发布，用户随后授权修复全新构建锁文件、核验开发记录并制作 6.4.2 补丁，见第 4.21 节、docs/releases/v6.4.2.md 和第 9 节。先核验实际标签、资产及 Windows 升级回执，不重复发布，不移动旧标签。请先读第 4.15–4.21 节和 Workbench 界面组合合同，不要重复恢复已完成批次。终点对照存在显示归零的观察网格敏感性；转入教学界面前明确声音代理与未触发展示，不能直接冻结真实操作标准。物理操作标准、触发及评分仍需同步实测交叉验证，不得通过模型自我证明；新增物理批次先报告证据、选项与风险。保持状态权威、持久化、撤销、模式保留、物理步进和采样不变，后续修改仍须完整回归并监视偶发长帧。

工作方式：小阶段验证无误且没有决策问题时可以连续推进；到大断点、存在行为歧义、缺少真实证据或准备扩大范围时再停下说明。每个大断点汇总全部修改、风险和验证结果，并先做本地 Git 提交。“本地保存”不等于上传。除非我明确要求上传，否则不要推送、不要发布、不要改版本号。若以后要上传，先检查 private、public-history 和 origin 的真实角色；本地最新内容始终是权威，严禁用远端直接覆盖本地。

请先向我简洁报告你读取到的当前状态、基线检查结果和准备开始的第一项具体工作；如果没有真正需要我决策的歧义，就直接开始执行到下一个大断点。
```
