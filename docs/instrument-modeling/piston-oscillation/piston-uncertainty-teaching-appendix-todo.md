# 活塞振动法自由模式：不确定度教学附录待办

确认日期：2026-09-22。课堂流程保留 10 题：A 类三步、质量与直径误差限换算两题、B 类传播一题、合成与报告四题。下列内容留作后续附录，不展开到学生计算正文或新增练习。

- [ ] 说明 Δm = 0.6 g、Δd = 0.1 mm 是沿用的误差限半宽，均匀分布换算 uB = Δ/√3；它们不代表分辨率。注明参数依据和非默认装置参数的教学适用范围。
- [ ] 说明 u仪器(P) = 100 Pa 是教学给定的标准不确定度，已包含显示分辨率等压力仪器影响；不再除以 √3，不再另加压力读数项。旧压力字段仅在存档迁移层辨认与清理，不再保存在当前仪器预算中。
- [ ] 区分仪器误差限、读数分辨率、标准不确定度和计算保护位。只有读数模型支持时，才能以半个显示步长作为均匀分布半宽。
- [ ] 记录标尺比例、时基比例、逐点高度读数、端点定位及采样率、跨越周期数在现有模型中的作用；不能以固定 uB(a) 代替随数据变化的结果。
- [ ] 记录 Hᵢ、Kᵢ 等逐点敏感系数与传播公式，不要求学生推导或逐项手算。
- [ ] 明确现有重叠处理是本课程的预算规则：u补(a)² = max(0, u读(a)² − uA(a)²)，不是通用的不确定度定律；保留模型适用条件和限制。
- [ ] 记录整组共用影响的处理，不随意除以 √n；说明高度零点、时间原点的抵消条件，以及直径与面积不能重复计入。
- [ ] 说明本实验以 h = aT² + b 的残差评定拟合斜率 A 类，不把不同高度当成同条件重复测量；记录 n ≥ 3、数据跨度及拟合近似的适用条件。
- [ ] 说明向 γ 传播时各项的对应关系与直径系数 2 的来源，记录独立性/相关性假设和详细推导。
- [ ] 说明 k = 2 的教学约定，避免无条件宣称 95% 覆盖概率；区分相对误差与相对标准不确定度。
- [ ] 精度方案仍待单独对齐：原始读数按分辨率，中间量按运算规则及必要保护位，最终按不确定度修约。本次未批准新的候选位数或舍入容限，暂沿用已有精度算法。

实现约束：上方维持“变量—数值—变量—数值”表格。uB(a) 在 uA(a) 完成核验后公开；其计算使用同一条公开数值链。所有学生提交并通过核验的数值、系统公开的中间结果、后续代入及报告一致。旧 19 题进度需明确提示重新计算，保留测量记录。
