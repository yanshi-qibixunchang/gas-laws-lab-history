# 气律实验室 6.4.2 补丁发行记录

> 日期：2026-09-15（Asia/Shanghai）
> 版本：6.4.2
> 状态：发行准备；最终状态以回执及远端 Release 为准。

## 修复范围

- 补齐构建工具可选 WASM 分支必需的 `@emnapi/runtime@1.11.3` 锁文件记录，修复新版 npm 全新安装失败；已有依赖版本保持不变。
- 新增全部必需 peer 的锁文件闭合检查，将 Windows 升级工作流的依赖安装门禁提前。
- 核验本轮新增开发记录，更新当前文档入口和过期版本背景，补齐架构组合合同索引；历史验收和模型数据保留原始结论。
- 更新包版本、三语言发布说明和生成型法律材料；应用状态、持久化格式、实验物理与显示值精确计算保持 6.4.1 行为。

## 检查与证据

- 修改前：严格 TypeScript、346 个测试文件、生产构建通过。
- npm 11.19.0 在隔离目录复现原锁文件缺项；只补必要记录后，实际全新安装 456 个包成功。新增锁文件回归覆盖 42 个必需 peer。
- 最终严格 TypeScript（含未使用符号）、347 个测试文件、生产构建、完整 NSIS 构建及三项资产门禁通过；生产与全依赖审计均为 0 漏洞。生成型法律清单为 468 条记录。
- 桌面包的 11 个 Electron 文件、运行依赖与版本说明同源码一致；导出器源码指纹保持不变，重新构建的产物哈希已同步。生成实验证据时只更新版本及来源指纹，实验 CSV 和数值汇总不变。
- `6.4.1 → 6.4.2` 实际增量重建与完整安装包逐字节相同，复制 79.91%、下载 20.09%。
- 5174 四类文件切换、标准启停与参数、理想气体高级设置、绝热演示控制、活塞引导／自由计划、结果窗口撤销重做及刷新恢复通过。标准边长 16、理想 0.80 和氦气计划均恢复，已移除的氦气说明框未出现；页面错误为 0。
- 实际 6.4.2 桌面包读取旧验收目录的边长 17，修改为 18 后正常关闭并重启仍为 18。桌面与浏览器关于窗口均显示 6.4.2，随附导出器检查可用。本补丁未重做完整三组物理采集或新的帧耗时量化对照。
- Windows `6.4.0 → 6.4.1` 补充升级以及 `6.4.1 → 6.4.2` 升级必须分别记录实际结果，不能用本地构建代替。

## 资产校验

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `heat-capacity-lab-setup-6.4.2.exe` | 182041244 | `e116d2ab84704e7874fdbcf1b227c7ef2fe10b4a3f97f018a24f0ed710c2f435` |
| `heat-capacity-lab-setup-6.4.2.exe.blockmap` | 191339 | `300ef3c5556b1c51a7fcdf49b7b94b0a8a60454051c7c5e7096f7347b2135b7d` |
| `latest.yml` | 6899 | `b990b2332a70e90ff5710996a08da6069f101901020707522f9e20fd31deb2d6` |

## 发布顺序与兼容边界

完整源码先更新用户选定的私有仓库，再同步匿名公开源码历史，最后发布公开安装更新资产。沿用 `Project Team` 公共提交身份、原日期和私有工作材料排除规则；公开发行仓库只包含发行资料及资产。

每个版本必须包含对应的 EXE、EXE.blockmap 和 latest.yml，且文件名、大小、SHA-512 一致。6.4.1 标签及资产保持原样；6.4.2 使用独立注释标签。应用 ID、安装目录规则、用户资料目录和自动更新仓库不变。

模型扫描不等于实物标定；此前观察到的开发预览偶发长帧继续保留为证据边界。本补丁没有修改运行时源码，也没有在用户日常环境执行安装、卸载或覆盖升级。
