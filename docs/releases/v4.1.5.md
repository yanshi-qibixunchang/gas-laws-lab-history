# 热容比实验室 v4.1.5

## 更新日志（简体中文）

### 修复

- **远端结构化更新说明**：测试修复后续版本更新说明的远端结构化读取与语言匹配。
- **更新弹窗版本信息区样式**：移除版本信息区的蓝色强调边和强调阴影，改为更简约的工程软件信息块。

### 兼容性说明

- **保留纯文本兜底**：远端 `latest.yml` 同时保留纯文本 `releaseNotes`，供旧版客户端和异常 fallback 使用。

## 更新日誌（繁體中文）

### 修復

- **遠端結構化更新說明**：測試修復後續版本更新說明的遠端結構化讀取與語言匹配。
- **更新彈窗版本資訊區樣式**：移除版本資訊區的藍色強調邊和強調陰影，改為更簡約的工程軟體資訊區塊。

### 相容性說明

- **保留純文字兜底**：遠端 `latest.yml` 同時保留純文字 `releaseNotes`，供舊版客戶端和異常 fallback 使用。

## Release Notes (English)

### Fixed

- **Remote structured release notes**: Tests the fix for remote structured release-note loading and language matching in future updates.
- **Update dialog version-information styling**: Removes the blue accent edge and emphasis shadow from the version-information block for a simpler engineering-software presentation.

### Compatibility

- **Plain-text fallback retained**: The remote `latest.yml` also keeps plain-text `releaseNotes` for older clients and fallback scenarios.
