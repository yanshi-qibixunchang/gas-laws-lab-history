# Design QA — 提示体系阶段 1（历史 B 方案基线）

> 本文件保留 2026-07-22 阶段一采用 B 方案时的视觉对照证据。用户已于 2026-07-23 将阶段一、阶段二窗口层切换为 A 方案；当前实现与验收应以 `docs/prompt-system-ui-spec.md` 及最新固定端口预览为准。

## 范围

- 目标：将已选定的 B 方案正式应用到通用确认窗口，并核对“关闭运行中的实验文件”真实业务状态。
- 参考状态：B 方案 / 警告型确认。
- 实现状态：标准模拟运行中 / 关闭文件确认。

## 对照材料

- 参考全屏：`docs/design-qa/prompt-phase1/source-b-warning-full.png`
- 参考窗口：`docs/design-qa/prompt-phase1/source-b-warning-dialog.png`
- 实现全屏：`docs/design-qa/prompt-phase1/implementation-close-running-final-full.png`
- 实现窗口：`docs/design-qa/prompt-phase1/implementation-close-running-final-dialog.png`
- 最终并排对照（左：参考；右：实现）：`docs/design-qa/prompt-phase1/comparison-dialog-final.png`
- 首轮对照留档：`docs/design-qa/prompt-phase1/comparison-dialog-initial.png`

## 捕获条件

- 参考与实现使用相同视口：1440 × 810 CSS px。
- 设备像素比：1。
- 最终中文警告窗口尺寸：参考 460 × 172 px，实现 460 × 172 px。
- 最终实现截图使用亮色主题、简体中文和真实运行中文件名。

## 视觉核对

- 结构一致：3 px 状态色条、48 px 标题栏、图标、眉题、标题、正文、后果说明、双操作按钮。
- 几何一致：宽度、高度、圆角、标题栏高度、正文与底栏分区均与 B 方案对齐。
- 色彩与层级一致：警告琥珀色、哑光面板、实体按钮、细边框与低扩散阴影符合选定方案。
- 文本适配：简体中文与繁体中文保持 460 × 172 px；英文因正文自然换行为 460 × 190 px，无横向溢出或裁切。
- 首轮发现 P2：正式实现缺少参考方案标题栏关闭按钮；第二轮已补齐并重新对照通过。

## 状态与交互核对

- 已检查真实入口：切换教学模式、重置自由模式未完成组、关闭运行中的实验文件。
- 初始焦点落在安全取消操作。
- `Tab` / `Shift+Tab` 焦点保持在窗口内；实测序列覆盖取消、确认、标题栏关闭。
- `Escape`、遮罩单击、标题栏关闭均按取消处理，并把焦点返回触发控件。
- 双击触发只保留一个确认窗口；第二次点击不会立即落到新遮罩并取消窗口。
- 遮罩阻止背景控件接收指针操作。
- 确认关闭运行中文件后，运行停止、打开标签关闭；控制台确认文件保留在本地缓存中。
- 无效实验流程中的“重置本组”继续直达原重置动作，不叠加第二层确认。

## 主题、语言与可访问性

- 手动检查：简体中文 / 亮色、English / 暗色、繁體中文 / 亮色。
- 三种语言均无横向溢出，安全按钮均获得默认焦点。
- `role="alertdialog"`、`aria-modal`、标题与描述关联完整。
- 浏览器控制台：0 error，0 warning。
- 减少动态效果偏好已由样式规则覆盖。

## 自动验证

- TypeScript 无输出类型检查通过。
- 220 个测试文件全部通过。
- 生产构建通过，且正式产物不含开发专用方案预览标记。
- 原生浏览器 `confirm` / `alert` / `prompt` 防回归扫描通过；桌面系统安全窗口保留。

final result: passed

---

# 活塞振动法软管第二转弯与远端方向 QA

## 对照基准

- 设计真值：`<local-user>\AppData\Local\Temp\codex-clipboard-bf0d4ba2-da0a-47bc-8426-06c326cc97e7.png`，并以用户在同一轮中最后明确的“画面俯视下顺时针约 40°”为方向标准。
- 浏览器实现：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\implementation-full.png`。
- 实现重点裁切：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\implementation-focus.png`。
- 并排对照（左：用户标注的原状态；右：修正后状态）：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\comparison.png`。
- 设计图像素：`874 × 576`。实现浏览器视口与全屏截图：`605 × 791` CSS/截图像素，`devicePixelRatio = 2`；三维画布内部为 `907 × 1186` 像素。
- 实现重点裁切：`605 × 576`；两侧以相同 576 px 高度并排，不对纵向密度做缩放。由于原图来自用户手动调整后的相机，横向视野不完全相同；判断只针对同一仪器状态下的软管投影方向与转弯轮廓，不对物体绝对坐标作伪精确比较。

## 全视图与重点区域

- 全视图检查确认临时预览页、真实传感器、黑色三脚架和桌面资产均保持原有结构；本轮只改变临时软管路径和对应说明文字。
- 重点并排对照覆盖传感器出口、桌面过渡、黑色脚架外侧第二转弯及远端延伸。原状态在第二转弯处存在向外鼓出的折点；修正后由切线连续的圆弧连接进入段和远端直线，不再出现折返或外凸尖角。
- 远端方向按固定确认相机的画面投影计算后顺时针偏转 40°，因此在确认视角中表现为向桌面深处偏右延伸；方向不会随用户临时旋转相机而重新计算。

## 必查表面

- 字体与排版：临时预览的标题、说明字号、行高和层级未改；路径说明仅更新方向语义，无截断或溢出。
- 间距与布局节奏：预览页布局、浮层边距和三维画布尺寸未改；软管与桌面、传感器及脚架的空间关系保持可读。
- 颜色与视觉变量：软管材质、桌面、传感器及脚架颜色未改，没有引入新的视觉变量。
- 图像质量与资产一致性：继续使用仓库中的真实三维资产和管状几何，没有替换为图片、占位模型或近似图形。
- 文案与内容：说明明确写为“在画面内顺时针偏转约 40°”，与实现的投影口径一致。

## 对照迭代历史

### 第一轮

- [P1] 第二转弯由多控制点样条生成，黑色脚架外侧出现不自然的向外鼓包。
- [P1] 远端沿旧相机投影方向近似直行，没有满足用户最终明确的顺时针约 40°视觉偏转。
- 修复：改用直线进入段、圆弧近似段和直线远端段组成的切线连续路径；按相机屏幕投影反算桌面内的真实三维方向。

### 第二轮

- 首次以世界坐标直接旋转 40°时，透视压缩会让画面中的远端接近水平，视觉偏转过量。
- 修复：将 40°应用于确认相机的二维画面投影，再反解回桌面方向；复核时远端呈向右上方延伸，且转弯不再外鼓。
- 未发现剩余 P0、P1 或 P2 问题。

## 交互与运行检查

- 已测试恢复确认视角、鼠标旋转、平移和滚轮缩放；软管世界位置在相机操作中保持不变。
- 临时预览加载状态为完成，浏览器控制台 `0 error`。
- 固定正式预览 `http://127.0.0.1:5174/` 不显示临时软管预览入口或内容。

## Findings

- 无剩余 P0、P1 或 P2 问题。

final result: passed

---

# 活塞振动法软管单弯合并与脚架间隙 QA

## 对照基准

- 设计真值：`<local-user>\AppData\Local\Temp\codex-clipboard-00975795-f1d2-4eb4-912c-378ecf0f3440.png`，以用户标注区域内“合并下降弯与转向弯、保留两端延伸方向、增大脚架间隙”为验收标准。
- 浏览器实现：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\implementation-single-bend-full.png`。
- 原状态重点裁切：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\source-two-bends-focus.png`。
- 实现重点裁切：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\implementation-single-bend-focus.png`。
- 并排对照（左：用户标注的双弯状态；右：单弯实现）：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-turn-qa\comparison-single-bend.png`。
- 设计图像素：`1904 × 1080`。实现浏览器视口为 `605 × 791` CSS px，`devicePixelRatio = 2`；三维画布内部为 `907 × 1186` 像素。两张重点图只比较软管轮廓、两端切向和脚架间隙，不对不同相机取景下的屏幕坐标作伪精确比较。

## 全视图与重点区域

- 传感器出口方向和远端延伸方向均沿用上一轮已确认结果；本轮只重构两者之间的弯曲过渡。
- 原路径可辨认出“向桌面下降”和“绕脚架转向”两个连续曲率峰值；实现改为一条五次贝塞尔曲线，使下降与转向成为同一连续动作。
- 数值采样确认曲率从入口端的零平滑上升，在 `t ≈ 0.486` 处达到唯一峰值，再平滑回落到出口端的零；与两端直线连接时无新增折点。
- 软管中心线至右侧脚架的最小距离由约 `31.6 mm` 增至约 `64.9 mm`；扣除脚架外廓和软管半径后的估算表面间隙由约 `3.1 mm` 增至约 `36.4 mm`，俯视下已有清楚留白。

## 必查表面

- 字体与排版：临时预览标题、说明层级和页面布局未改。
- 间距与布局节奏：只增加软管与右侧脚架之间的三维间隙，未改变预览浮层或画布布局。
- 颜色与视觉变量：软管、传感器、脚架和桌面材质均未改。
- 图像质量与资产一致性：继续复用仓库真实三维资产；未修改 GLB、正式模型或整机尺寸。
- 文案与内容：路径说明更新为“同一条连续曲线中下降与转向”，与当前实现一致。

## 对照迭代历史

### 第一轮

- [P1] 下降弯与转向弯各自形成明显低曲率大弯，俯视轮廓呈两个连续鼓包。
- [P2] 管线经过右侧脚架时仅有约 `3.1 mm` 的估算表面空隙，视觉上过于贴近。
- 修复：用端点二阶导数为零的单条五次贝塞尔曲线合并两个弯，并将弯曲区向脚架外侧推移；入口与远端切向保持不变。

### 第二轮

- 并排复核显示新版只有一处宽缓转向，原有双弯轮廓已消失；脚架与软管之间形成稳定可见空隙。
- 未发现剩余 P0、P1 或 P2 问题。

## 交互与运行检查

- 已测试确认视角、鼠标旋转、平移和滚轮缩放；曲线在不同观察角度下保持连续，未出现相交或跳变。
- 临时预览加载完成，浏览器控制台 `0 error`。
- 类型检查、活塞振动相关测试、正式构建和差异格式检查均通过；固定正式预览 `http://127.0.0.1:5174/` 未出现临时预览内容。

## Findings

- 无剩余 P0、P1 或 P2 问题。

final result: passed

---

# 活塞振动法软管快速下降与可拆接头 QA

## 对照基准

- 设计真值：用户本轮明确的高度轨迹要求——出口切线先水平，下降斜率绝对值先增大后减小，在约原路径二分之一至三分之一内与桌面相切；桌面投影方向全部保持不变；软管仪器端必须带有随管取下的白色旋转卡扣接头。
- 投影参考截图：`<local-user>\AppData\Local\Temp\codex-clipboard-00975795-f1d2-4eb4-912c-378ecf0f3440.png`。
- 快速下降实现：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-drop-connector-qa\implementation-fast-descent.png`。
- 可拆接头实现：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-drop-connector-qa\implementation-detached-connector.png`。
- 合并对照：`D:\program\Codex\Home\visualizations\2026\08\14\019fffe9-58e9-7142-9458-8cd58c04841e\piston-hose-drop-connector-qa\comparison-fast-descent-and-connector.png`。
- 投影参考图为 `1904 × 1080` 像素；实现浏览器视口与截图为 `1280 × 720` CSS/截图像素，`devicePixelRatio = 2`。参考图为俯视状态，实现图分别为下降侧视和接头侧视，因此只对投影方向、下降轮廓和接头资产作对应判断，不比较不同相机下的绝对屏幕坐标。

## 全视图与重点区域

- 下降高度改用独立于桌面投影的五次平滑高度曲线；所有 `x/z` 控制点保持上一轮确认值，因此桌面俯视投影、脚架间隙和远端方向没有变化。
- 高度曲线在传感器出口与贴桌点的一阶、二阶导数均为零，下降斜率绝对值只出现一个峰值；完成下降的平面路径为约 `86.0 mm`，占原完整弯曲路径约 `33.39%`。
- 软管末端复用原 GLB 中的 `Connector_Main_White`、`Connector_Main_RotatingCollar`、六道黑色卡纹和螺纹芯；仪器侧 `Port_Main` 与密封圈未随软管复制。
- 接头沿软管远端切线放置，软管在末端以平滑高度过渡抬升到接头轴线；估算接头最低点高于桌面约 `0.35 mm`，未穿透桌面。

## 必查表面

- 字体与排版：预览页原有字体、字号、行高和层级未改；说明增加一条接头状态，未出现截断或溢出。
- 间距与布局节奏：页面与画布布局未改；仅改变软管高度轨迹并增加末端实体。
- 颜色与视觉变量：继续使用原模型白色接头、黑色卡纹和螺纹芯材质，没有新增近似配色。
- 图像质量与资产一致性：接头直接复用正式 GLB 的真实网格和材质，不使用占位几何、图片或近似绘制；正式 GLB 本身未修改。
- 文案与内容：路径说明同步写明“约原路径三分之一内快速贴桌”及“接头随管取下、固定口不移动”。

## 对照迭代历史

### 第一轮

- [P1] 软管高度在整条弯曲过渡中缓慢下降，贴桌距离过长，不符合软管受重力快速下垂的物理观感。
- [P1] 断开状态只有软管裸端，缺少真实实验中随软管一起取下的白色旋转卡扣接头。
- 修复：将高度变化从平面转向中解耦，用一段五次平滑曲线在约三分之一路径内完成下降；从原 GLB 提取可移动快拆组件并绑定到软管末端切线。

### 第二轮

- 侧视复核确认软管从水平切线离开后快速下降，并以水平切线贴桌；未出现折角、二次坡峰或桌面穿透。
- 接头侧视确认白色主体、旋转锁环、黑色卡纹与软管连续连接，并以自然高度平放桌面。
- 未发现剩余 P0、P1 或 P2 问题。

## 交互与运行检查

- 已测试恢复确认视角、鼠标旋转、平移和滚轮缩放；软管与接头在视角变化中保持连接，无跳变。
- 临时预览加载完成，浏览器控制台 `0 error`。
- 类型检查、6 个活塞振动测试文件、正式构建和差异格式检查均通过；固定正式预览 `http://127.0.0.1:5174/` 与临时预览 `http://127.0.0.1:5184/?pistonHosePreview=1` 均返回 `200`。

## Findings

- 无剩余 P0、P1 或 P2 问题。

final result: passed

---

# 产品介绍页英文标签与纵向重心 QA

## 对照基准

- 设计真值：`<local-user>\AppData\Local\Temp\codex-clipboard-397d8c7b-0c47-4eec-838a-3b7190b846b5.png`
- 归一化设计图：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\source-normalized.png`
- 浏览器实现：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\implementation-en-page2.png`
- 全视图并排对照：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\comparison-normalized.png`
- 学习路径重点对照：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\path-focused-comparison.png`
- 回放入口问题截图：`<local-user>\AppData\Local\Temp\codex-clipboard-d3a13b5b-7e5f-462e-bbaa-4d31d57d15c1.png`
- 回放入口修复截图：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\replay-en-page2-finish-fixed.png`
- 回放入口全视图对照：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\replay-en-full-comparison.png`
- 回放入口学习路径对照：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-balance-qa\replay-en-path-comparison.png`
- 对照状态：浅色主题、英文、产品介绍第 `02 / 03` 页、自动播放暂停。
- 浏览器视口与实现截图：`2048 × 1152` CSS px，`deviceScaleFactor = 1`，实现截图像素为 `2048 × 1152`。
- 设计图原始像素为 `2408 × 1364`；去除顶部 24 px 外部黑边后按宽度归一化为 `2048 × 1139`，再向下补齐至 `2048 × 1152`。设计图和实现图以相同像素尺寸并排比较。

## 全视图与重点区域

- 三页共用标题系统的高度、左右边界和页码位置保持不变；产品介绍内容整体向下回落，标题、主展示、学习路径、页码/播放控制和底部操作区形成由紧到松的纵向节奏。
- 第二页三栏展示与说明区没有被缩小；学习路径下移后仍与页码状态区保留明确间隔，第一页工作台演示和第三页结果演示也没有越过状态区。
- 英文 `Learning path` 标签由中文用的方形尺寸改为英文专属横向尺寸，双行文字四周留白均匀，并保持在居中的路径文案左侧，不参与路径正文的居中计算。
- 重点区域使用独立裁切对照，因为本轮主要差异集中在英文标签的宽高、文字呼吸空间及学习路径的垂直位置。

## 必查表面

- 字体与排版：继续使用现有产品标题、数据和界面字体；英文标签字号、行高和字距未改变层级，仅缩小字距并增加容器宽度，避免字形贴边。
- 间距与布局节奏：产品页顶部内边距由 `14px` 调整为 `34px`；展示主体使用上 `24px`、下 `4px` 的不对称内边距，使视觉重心下移，同时不改变三页标题轨道。
- 颜色与视觉变量：标签、背景、边框、阴影和强调文字全部继续使用现有工作台变量，没有新增渐变、玻璃或重阴影。
- 图像质量与资产一致性：三栏继续复用原生三维场景和现有录制/实时结构，没有替换资产、拉伸图片或新增近似占位图。
- 文案与内容：简体中文、繁体中文和英文文案保持原有副本；仅英文标签使用 `84 × 56px` 的专属尺寸，窄窗口下降为 `64 × 42px`。

## 对照迭代历史

### 第一轮

- [P2] 英文 `Learning path` 仍沿用中文 48 px 方形标签，双行英文与边界过近，显得局促。
- [P2] 去除顶部工作台菜单后，标题和三页主要内容整体停留在偏上的旧位置，画面上轻下空，视觉重心偏高。
- 修复：增加英文专属标签宽高和窄窗口尺寸；统一增加产品介绍页顶部空间，并重新分配三页主体的上下内边距。

### 第二轮

- 全视图对照显示标题和主要演示整体下移约 20–30 px（归一化视图），底部学习路径与状态区之间仍有安全间隔。
- 重点对照显示英文标签已有稳定的水平和垂直内边距，正文路径仍保持独立居中。
- 未发现剩余 P0、P1 或 P2 问题。

### 第三轮（设置中的产品介绍回放入口）

- [P2] 首轮复核只覆盖首次启动容器；设置中的“重新观看产品介绍”覆盖层没有携带语言标记，导致英文专属规则未命中，仍回退到中文方形标签。
- 修复：回放覆盖层补齐当前语言标记，同时把语言标记直接下沉到学习路径组件，使标签尺寸不再依赖外层入口是否正确传递语言。
- 复核：在与用户截图一致的 `2426 × 1366`、英文、回放第 `02 / 03` 页、底部按钮为 `Finish` 的状态下，标签计算尺寸为 `84 × 56px`，页面溢出为 `0`；重点并排对照确认 `Learning` 已完整落在边框内并具有稳定的左右留白。

## 响应式、交互与控制台

- 已检查 `2426 × 1366`、`2048 × 1152` 与 `820 × 600` 三种视口；窄窗口下英文标签为 `64 × 42px`，页面横向和纵向溢出均为 `0`。
- 已检查简体中文、繁体中文和英文第二页；中文标签继续使用原方形比例，英文使用专属横向比例。
- 已切换并检查产品介绍第 1、2、3 页；三页标题起点一致，主展示没有覆盖页码、播放控制或底部按钮。
- 浏览器控制台：`0 error`、`0 warning`。

final result: passed

---

# 产品介绍第二页三语言等价性 QA

## 对照基准

- 设计真值：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-localization-qa\product-intro-page2-zh-cn-reference.png`
- 英文实现：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-localization-qa\product-intro-page2-en-three-modes.png`
- 合并对照：`D:\program\Codex\Home\visualizations\2026\07\22\019f877c-37eb-7891-9af8-e5db686d482b\onboarding-localization-qa\product-intro-page2-comparison.png`
- 浏览器视口与截图像素：`1037 × 743` CSS px，`deviceScaleFactor = 1`；两张图无需密度换算。
- 对照状态：浅色主题、产品介绍第 `02 / 03` 页、自动播放暂停稳定帧。

## 全视图与重点区域对照

- 简体中文继续使用三模式录制视频；英文和繁体中文使用三套原生界面同步渲染。三者均保持同样的三栏结构、三段模式说明和学习路径。
- 英文实现按“Demo mode / Guide mode / Free mode”从左到右排列，三栏宽度、间距、标题区高度、说明区边界和学习路径位置与简体中文设计真值一致。
- 英文和繁体中文的三个视图共享同一 `controlledElapsedMs`，进入模式、清单勾选、自动演示和自由操作不会出现各栏时间漂移。
- 重点区域已检查：三种模式选择条、演示步骤面板、引导清单、自由模式聚焦面板、三段说明及底部学习路径。

## 必查表面

- 字体与排版：沿用产品介绍现有字体系统；英文长文案在说明格内两行收束，无截断或覆盖。
- 间距与布局节奏：三栏使用相同网格和 `11px` 间距，整体比例与录制版一致，导航箭头及底部操作区未移位。
- 颜色与视觉变量：继续使用既有工作台表面、边框、强调色与阴影变量，没有新增视觉体系。
- 图像质量与资产一致性：三栏直接复用原生 `HeatCapacityInstrumentScene`，没有替换成占位图、CSS 绘图或近似模型。
- 文案与内容：简体中文、繁体中文、英文分别使用对应副本；繁体统一使用“示範模式”，英文无中文残留。

## 对照迭代历史

### 第一次对照

- [P1] 英文和繁体中文仅显示单个引导模式视图，缺少演示、自由模式及整页说明结构。
  - 修复：新增三模式同步实时容器，复用录制页的 `demo / guide / free` 三视图组合，并抽取三模式说明和学习路径作为视频版与实时版的共用内容。
  - 复核：英文、繁体中文均检测到三个模式面板，顺序固定为 `demo / guide / free`；简体中文仍检测到一个三栏录制视频、三段说明和一套学习路径。

### 第二次对照

- 未发现剩余 P0、P1 或 P2 问题。
- 实时版与录制版的帧内容会因语言和时间点不同而变化，这是预期差异；结构、比例、层级和操作语义保持一致。

## 交互与控制台

- 已测试：三种语言进入第二页、上一页/下一页、暂停/继续、三栏同步推进，以及演示/引导/自由三种面板状态。
- 英文和繁体中文均检测到三个模式面板；同步时间值一致。
- 浏览器控制台：`0 error`、`0 warning`。

## Findings

- 无剩余 P0、P1 或 P2 问题。

final result: passed

---

# 首次流程欢迎页视觉 QA

## 对照基准

- 设计真值：`D:\program\Codex\Home\generated_images\019f877c-37eb-7891-9af8-e5db686d482b\exec-43be8be3-4480-460f-a1ea-8e54ae221a8f.png`
- 实现截图：`<project-root>\output\playwright\first-run-welcome-zh-CN.png`
- 合并对照：`<project-root>\output\playwright\first-run-welcome-comparison.png`
- 窄窗口截图：`<project-root>\output\playwright\first-run-welcome-en-narrow.png`
- 产品介绍顶部验证：`<project-root>\output\playwright\first-run-product-top-clean.png`
- 许可窗口验证：`<project-root>\output\playwright\first-run-consent-top-clean.png`
- 目标状态：浅色主题、首次启动、简体中文欢迎动画稳定帧。
- 浏览器视口：`1600 × 900` CSS px，`deviceScaleFactor = 1`。
- 设计图原始像素：`1672 × 941`；按同一纵横比归一化为 `1600 × 900`。
- 实现截图像素：`1600 × 900`；无需密度换算。

## 全视图对照

设计图与实现截图已归一化后横向合并检查。最终实现保留了选定方案的左侧放大应用标志、竖向细分隔线、右侧左对齐品牌名、主标题、短横线和说明文字。整体可见区域与设计图重心一致；实现使用项目已有的纯色主题背景，不复制生成图中轻微的图像噪声和明暗变化。

## 重点区域对照

- 欢迎组合：实现内容框约 `956 × 256` px，应用标志约 `187 × 187` px，竖向分隔线位于 `x ≈ 589`，中文标题起点位于 `x ≈ 670`，与归一化设计图的构图位置一致。
- 顶部区域：语言、欢迎、产品介绍和许可阶段均无工作台菜单项；标题栏背景与内容背景均为 `rgb(233, 237, 241)`，下边框为 `0px`。
- 多语言：简体中文、繁体中文、英文在 `1600 × 900` 下均保持单行，无横向或纵向溢出；英文标题宽约 `577px`，说明宽约 `577px`，位于约 `608px` 的文字轨道内。
- 响应式：英文在 `600 × 700` 下转为上下排版，无溢出，标题和说明自然换行。
- 许可窗口：缩放后的窗口完整落在视口内，最终边界约为 `top 53px / bottom 847px`。

## 必查表面

- 字体与排版：使用项目已授权并内置的 Noto Sans SC、Gas Laws Lab Serif 与现有字体回退；中文标题 `52px`，英文标题 `44px`，字号、行高和多语言收缩均无截断。
- 间距与布局节奏：欢迎组合宽度、图标尺寸、双侧间距、分隔线高度和文字起点已按设计图二次收紧；标准和窄窗口均无滚动溢出。
- 颜色与视觉变量：沿用 `--studio-bg`、`--studio-text`、`--studio-muted` 和现有蓝色强调色；顶部与内容区背景完全一致，无新增渐变、玻璃效果或重阴影。
- 图像质量与资产一致性：直接复用产品原有 `favicon.png`，没有以 CSS、内联 SVG、文字图形或占位资源替代标志；浏览器缩放下边缘清晰。
- 文案与内容：简体中文、繁体中文和英文均直接使用现有 `firstRunCopies`，未增加虚构说明或营销文案。

## 对照迭代历史

### 第一次对照

- [P2] 顶部标题栏仍计算为浅色表面色，与页面背景有细微色差。
  - 修复：提高首次流程标题栏选择器优先级，并明确使用 `var(--studio-bg)`、移除下边框。
  - 复核：标题栏与内容区计算色均为 `rgb(233, 237, 241)`。
- [P2] 初版欢迎组合相较设计图偏小且横向过散。
  - 修复：容器由 `1080px` 收紧为 `860px`，图标由 `150px` 放大为 `168px`，中文标题由 `46px` 放大为 `52px`，间距由 `56px` 调整为 `72px`。
  - 复核：最终图标、分隔线与标题坐标已与归一化设计图对齐。
- [P1] 许可窗口在固定比例工作台缩放后顶部被裁切。
  - 修复：将许可窗口最大高度改为相对于覆盖层的 `min(742px, calc(100% - 48px))`，并提高选择器优先级。
  - 复核：窗口完整位于 `1600 × 900` 视口内。

### 第二次对照

- 未发现可执行的 P0、P1 或 P2 差异。
- 可接受差异：生成设计图存在轻微光照/噪声质感，实现坚持产品既有纯色工程软件背景；该差异属于设计系统约束，不需要修复。

## 交互与控制台

- 已测试：三种语言切换、语言页进入欢迎页、欢迎页进入产品介绍、产品介绍进入使用确认、选择答案后打开强制许可窗口。
- 产品介绍页顶部从 `y = 0` 开始，未保留工作台菜单条。
- 浏览器控制台：`0 error`、`0 warning`。
- 类型检查、全量测试和正式构建均通过。

## Findings

- 无剩余 P0、P1 或 P2 问题。

## Follow-up Polish

- P3：如后续希望更接近生成图，可再微调图标圆角和品牌名的字间距；当前差异不影响层级、空间利用率或多语言稳定性。

final result: passed

---

# 活塞振动法整机尺寸预览：上方夹持结构与气路接口 QA

## 对照基准

- 设计真值：`<local-user>\AppData\Local\Temp\codex-clipboard-33555128-462f-4e53-a1f2-a998cd2b13e8.png`（上方夹持结构脱离本体）。
- 设计真值：`<local-user>\AppData\Local\Temp\codex-clipboard-faeeb42e-bceb-4f42-a3aa-db0089aed42a.png`（软管从白色接头侧向穿出的错误观感）。
- 实现地址：`http://127.0.0.1:5184/?pistonModelSizePreview=1`，状态为“校正后”。
- 目标视口：`1716 × 1240` CSS px，设备像素比 `1`。
- 实现截图：未能取得有效三维画面。当前应用内浏览器的自动化捕获环境无法创建 `webgl` 或 `webgl2` 上下文，截图只包含页面覆盖层；该图不能作为三维实现证据。

## 本轮修复

- 上方原有 `RodClampBridge`、`RodClampKnob`、`RodClampMount` 及其交互轴随本体高度校正向下移动，继续沿未缩放的支撑杆工作，不引入新的上方零件。
- 修复后防护框顶板中心与夹持桥中心的世界坐标高度差为约 `0.000007 mm`，消除了缩放后约 `82.75 mm` 的竖向脱离。
- 外部软管在既有起点后增加沿白色快拆接头轴线的短直引出段，再平滑转入原有路径；数值复核的初始切向与世界 `+Y` 轴点积为 `0.99999998`。
- 软管中心线起点位于缩放后接头顶部以内约 `2.413 mm`，保证管壁与接口实体连续相接，而不是从侧面悬空或穿出。

## 必查表面

- 字体与排版：本轮未改页面文字、字号、行高或层级。
- 间距与布局节奏：本轮只修复三维节点的联动高度和接口附近曲线，不改页面布局。
- 颜色与视觉变量：本轮未改材质、颜色、光照或页面变量。
- 图像质量与资产一致性：继续复用仓库正式 GLB 中的夹持结构、白色接口和其他仪器资产；仅由现有软管材质生成校正后的管状几何。
- 文案与内容：本轮未改说明文案。

## 自动验证

- 临时预览加载状态为 `ready=true`、模式为 `corrected`，控制台无 error 或 warning。
- TypeScript 无输出类型检查通过。
- 6 个活塞振动法测试文件全部通过。
- 正式构建和差异格式检查通过。
- 固定预览 `http://127.0.0.1:5174/` 与临时预览均返回 HTTP 200。

## Findings

- [P1] 最终三维视觉对照仍需人工确认。
  - Evidence：两张用户标注截图已打开；自动化实现截图因捕获环境没有 WebGL 上下文而为空，无法把同视角实现与真值拼接比较。
  - Impact：几何关系和运行状态已验证，但不能据此声称最终屏幕观感已经通过。
  - Fix：由用户在已打开的 WebGL 预览中检查上方连接和接口出管；确认后补记通过，若仍有偏差则继续同一阶段迭代。

## 对照迭代历史

- 第一轮：从用户截图确认两个 P1 问题——上方夹持组件未随本体降高、软管起始切向错误。
- 修复后：结构坐标与曲线切向的数值复核通过；浏览器运行和工程验证通过，但三维截图对照受 WebGL 捕获能力阻断。
- 用户验收：2026-08-16，用户在本地 WebGL 预览中明确确认该阶段可以进入下一阶段；自动化 QA 仍因缺少可保存的有效实现截图保持 blocked，不影响本次产品阶段检查点。

final result: blocked

---

# 活塞振动法整机模型：毫米刻度与气缸行程校准 QA

## 对照基准

- 产品真值：实验正式操作高度为 `80、70、60、50、40、30、20 mm`；刻线保留至 `85 mm`，数字只标 `10–80`，读数基准为石墨活塞下沿。
- 前置基线：用户于 2026-08-16 在本地 WebGL 预览中确认整机尺寸阶段，可以在该版本上继续。
- 实现地址：`http://127.0.0.1:5184/?pistonModelSizePreview=1`，默认状态为“刻度校准”。
- 实现截图：自动化捕获环境仍不能提供有效 WebGL 三维画面，因此不把空白截图作为实现证据。

## 本轮实现

- 复用原 GLB 三档真实刻线网格，按真实 `1 mm` 间距生成 `0–85 mm` 共 `86` 条刻线：`10 mm` 主刻线 9 条、`5 mm` 中刻线 9 条、其余次刻线 68 条。
- 保留原有 `10–80` 数字并按毫米间距重排，隐藏 `90` 数字；`85 mm` 保留无数字刻线。
- 以石墨活塞下沿为读数基准，默认把活塞组件置于最高的 `85 mm` 刻线，没有加入正式高度拖动交互。
- 在不压缩刻度间距、不移动 `85 mm` 刻线的前提下，于活塞上限之上增加 `10 mm` 无刻度玻璃管留白；联动延长四面防护框并抬高上导向环、顶板、顶部后框与既有夹持桥。侧面活塞锁紧螺钉留在下一阶段。
- 预览提供“刻度校准 / 尺寸阶段 / 原模型”三档同页切换，便于按同一相机视角比较阶段差异。

## 数值复核

- `10–80` 数字的世界坐标高度差为 `70.000 mm`。
- `90` 数字节点保持存在但不可见；生成刻线总数为 `86`，并包含无数字的 `85 mm` 中刻线。
- 默认石墨活塞下沿位于刻度零点以上 `85.000 mm`，直接对准最高刻线。
- 在该 `85 mm` 上限状态下，活塞顶部与上导向环底部距离为 `10.700 mm`，其中 `10 mm` 为本轮待视觉验收的新增留白，`0.700 mm` 为运行余量。
- 玻璃缸顶部、防护框四面板顶部和顶板底面在数值误差范围内齐平，未发现结构脱节。

## 自动验证

- TypeScript 无输出类型检查通过。
- 全量 `237` 个测试文件通过。
- 正式构建通过，构建产物策略检查通过。
- `git diff --check` 通过。
- 固定预览 `http://127.0.0.1:5174/` 与临时预览均返回 HTTP 200。

## Findings

- [P1] 刻度密度、数字可读性、顶部留白和整机缩短后的最终三维观感仍需用户在有效 WebGL 预览中确认。
  - Evidence：几何数量、毫米映射、结构边界和运行状态均已数值复核；自动化环境不能保存有效三维截图。
  - Impact：可以确认物理映射和结构连续性，但不能代替人眼判断刻度是否清楚、顶部比例是否自然。
  - Fix：由用户检查默认“刻度校准”状态及与“尺寸阶段”的同视角对照；确认后再固化本阶段，未确认前不加入侧面锁紧螺钉。

## 用户验收

- 2026-08-16：用户确认 `85 mm` 上限、额外 `10 mm` 无刻度玻璃管留白、抬高后的顶部黑色板与延长后的支撑柱可以进入下一阶段。
- 本阶段作为静态模型检查点固化；自动化 QA 仍因缺少可保存的有效 WebGL 实现截图保持 blocked，不影响产品阶段验收结论。

final result: blocked

---

# 活塞振动法整机模型：侧面锁紧螺钉静态预览 QA

## 对照基准

- PASCO 官方手册：`https://cdn.pasco.com/product_document/Heat-Engine-Gas-Law-Apparatus-Manual-TD-8572A.PDF`，首张结构图把 `Locking Screw` 列为 TD-8572A 本体部件。
- PASCO 官方产品页：`https://www.pasco.com/products/lab-apparatus/thermodynamics/heat-engine/td-8572`，明确说明锁紧螺钉用于固定活塞，并区分于整机支撑杆夹具。
- 仓库固定规范：螺钉安装在气缸上部保护框架侧面，水平径向指向活塞杆；外露部分为短圆柱形手拧头，旋转时应伴随少量轴向进退。
- 外部事实限制：公开资料未提供准确螺纹规格、轴向行程、接触端形状和安装尺寸，因此本断点不把这些推定值作为正式物理规格。

## 本轮实现

- 在顶部黑色保护板右侧增加独立锁紧螺钉组件，轴线沿世界 `X` 方向并精确穿过活塞杆中心线。
- 静态组件包括：约 `13 mm` 直径的多棱手拧头、三道握持环、螺纹座、金属轴和内侧锥形接触端。
- 默认状态为 `loose`；组件带独立 `AXIS_PistonLockingScrew` 运动轴和状态元数据，但所有网格暂不接受命中。
- 预览增加“锁紧螺钉 / 刻度校准 / 尺寸阶段 / 原模型”四档对照；只有首档显示新增部件。

## 数值复核

- 螺钉轴线与顶部黑色板中心高度差为 `0.000 mm`，与活塞杆中心线横向偏差为 `0.000 mm`。
- 松开状态的内侧接触端与活塞杆外表面保持 `1.000 mm` 间隙，没有提前锁住或穿入活塞杆。
- 手拧头直径为 `13.000 mm`，整套组件在保护板外侧伸出约 `13.000 mm`。
- 螺纹轴隐藏在顶部保护板内部，外露手钮与安装座连续连接；组件没有进入玻璃缸的可见工作区。

## 自动验证

- TypeScript 无输出类型检查通过。
- 6 个活塞振动法测试文件通过。
- 正式构建和构建产物策略检查通过。
- `git diff --check` 通过。
- 固定预览 `http://127.0.0.1:5174/` 与临时预览均返回 HTTP 200。

## Findings

- [P1] 手拧头尺寸、外伸长度、安装侧和默认整机视角下的可见性需要用户在有效 WebGL 预览中确认。
  - Evidence：官方资料确认部件功能和大致结构，但没有精确尺寸；当前数值是按现有一比一模型比例建立的视觉候选。
  - Impact：未确认前不能固化为正式模型，也不应继续制作旋转进退动效。
  - Fix：由用户审查默认“锁紧螺钉”状态；确认位置和比例后再固化本阶段并进入松紧两端状态与运动轨迹。

## 用户验收

- 2026-08-16：用户确认锁紧螺钉的安装侧、静态位置、外伸长度和整体比例可以继续。
- 本阶段固化为静态模型检查点；下一阶段只增加松紧端点和旋转—进退轨迹，不提前接入实验流程状态机。

final result: blocked

---

# 活塞振动法整机模型：锁紧螺钉动作轨迹预览 QA

## 本轮实现

- 将锁紧螺钉拆分为固定螺纹座与可动总成；固定座不随操作移动。
- 可动总成包含手钮、螺纹轴和接触端，沿既定水平轴线同步旋转和轴向进给。
- 增加“完全松开”“完全旋紧”端点以及可逐帧拖动的动作轨迹审查控件。
- 松开端保留 `1 mm` 接触间隙；旋紧端轴向前进 `1 mm`，接触端恰好到达活塞杆表面而不穿入。
- 手钮端面增加小型旋转观察标记，便于在外形基本不变的情况下判断旋转方向和圈数。

## 边界说明

- `1.5` 圈是便于本轮观察的临时动作值，不作为正式螺纹规格。
- 右侧按钮和滑杆只是临时审查控件，不代表正式实验中的鼠标交互方案。
- 本轮不加入命中区域、操作音效、锁紧判定或活塞移动约束。

## 自动验证

- TypeScript 无输出类型检查通过。
- 6 个活塞振动法测试文件通过。
- 正式构建和构建产物策略检查通过。
- `git diff --check` 通过。
- 固定预览 `http://127.0.0.1:5174/` 与临时预览 `http://127.0.0.1:5184/?pistonModelSizePreview=1` 均返回 HTTP 200。

## Findings

- [P1] 旋转方向、临时圈数、轴向进给的视觉强度以及旋紧终点的接触观感仍需用户在有效 WebGL 预览中确认。
  - Impact：未确认前不固化动作轨迹，也不继续接入正式交互与实验状态判定。
  - Fix：拖动临时预览右侧的“旋转—进退轨迹”滑杆，分别检查中间状态和两个端点。

## 用户验收

- 2026-08-16：用户确认锁紧螺钉的松紧端点、旋转—进退轨迹和接触终点检查无误。
- 本阶段固化为动作轨迹检查点；最终鼠标手势、声音和状态机仍按计划延期。

final result: blocked
