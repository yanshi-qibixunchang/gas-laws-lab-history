# Design QA — 提示体系阶段 1（历史 B 方案基线）

> 本文件保留 2026-07-22 阶段一采用 B 方案时的视觉对照证据。用户已于 2026-07-23 将阶段一、阶段二窗口层切换为 A 方案；当前实现与验收应以 `docs/prompt-system-ui-spec.md` 及最新固定端口预览为准。

## 范围

- 目标：将已选定的 B 方案正式应用到通用确认窗口，并核对“关闭运行中的实验文件”真实业务状态。
- 参考状态：B 方案 / 警告型确认。
- 实现状态：标准模拟运行中 / 关闭文件确认。

## 对照材料

- 参考全屏：`docs/design-qa/prompt-phase1/source-b-warning-full.png`
- 参考窗口：`docs/design-qa/prompt-phase1/source-b-warning-dialog.png`
- 实现全屏：`docs/design-qa/prompt-phase1/implementation-close-running-final-full.png`
- 实现窗口：`docs/design-qa/prompt-phase1/implementation-close-running-final-dialog.png`
- 最终并排对照（左：参考；右：实现）：`docs/design-qa/prompt-phase1/comparison-dialog-final.png`
- 首轮对照留档：`docs/design-qa/prompt-phase1/comparison-dialog-initial.png`

## 捕获条件

- 参考与实现使用相同视口：1440 × 810 CSS px。
- 设备像素比：1。
- 最终中文警告窗口尺寸：参考 460 × 172 px，实现 460 × 172 px。
- 最终实现截图使用亮色主题、简体中文和真实运行中文件名。

## 视觉核对

- 结构一致：3 px 状态色条、48 px 标题栏、图标、眉题、标题、正文、后果说明、双操作按钮。
- 几何一致：宽度、高度、圆角、标题栏高度、正文与底栏分区均与 B 方案对齐。
- 色彩与层级一致：警告琥珀色、哑光面板、实体按钮、细边框与低扩散阴影符合选定方案。
- 文本适配：简体中文与繁体中文保持 460 × 172 px；英文因正文自然换行为 460 × 190 px，无横向溢出或裁切。
- 首轮发现 P2：正式实现缺少参考方案标题栏关闭按钮；第二轮已补齐并重新对照通过。

## 状态与交互核对

- 已检查真实入口：切换教学模式、重置自由模式未完成组、关闭运行中的实验文件。
- 初始焦点落在安全取消操作。
- `Tab` / `Shift+Tab` 焦点保持在窗口内；实测序列覆盖取消、确认、标题栏关闭。
- `Escape`、遮罩单击、标题栏关闭均按取消处理，并把焦点返回触发控件。
- 双击触发只保留一个确认窗口；第二次点击不会立即落到新遮罩并取消窗口。
- 遮罩阻止背景控件接收指针操作。
- 确认关闭运行中文件后，运行停止、打开标签关闭；控制台确认文件保留在本地缓存中。
- 无效实验流程中的“重置本组”继续直达原重置动作，不叠加第二层确认。

## 主题、语言与可访问性

- 手动检查：简体中文 / 亮色、English / 暗色、繁體中文 / 亮色。
- 三种语言均无横向溢出，安全按钮均获得默认焦点。
- `role="alertdialog"`、`aria-modal`、标题与描述关联完整。
- 浏览器控制台：0 error，0 warning。
- 减少动态效果偏好已由样式规则覆盖。

## 自动验证

- TypeScript 无输出类型检查通过。
- 220 个测试文件全部通过。
- 生产构建通过，且正式产物不含开发专用方案预览标记。
- 原生浏览器 `confirm` / `alert` / `prompt` 防回归扫描通过；桌面系统安全窗口保留。

final result: passed

---

# Design QA — 产品介绍第二页引导模式视频化试制

## 本轮范围

- 仅试制第二张介绍页中的“引导模式”画面；演示模式与自由模式尚未制作成视频。
- 正式试制路径限定为简体中文、亮色主题；繁体中文、英文或暗色主题暂时继续使用原有实时演示。
- 页面级标题、`02 / 03`、左右翻页、暂停 / 继续与底部操作仍由实时界面负责；只有中央原生实验画面改为本地静音视频。

## 同画布视觉对照

- 实时源帧：`outputs/design-qa/product-intro-guide-live-source-frame-2s.png`，时间点 2.000 秒，1050 × 750 px。
- 编码后同时间点：`outputs/design-qa/product-intro-guide-video-frame-2s.png`，1050 × 750 px。
- 并排对照：`outputs/design-qa/product-intro-guide-video-source-vs-encoded.png`；左侧为原生实时源帧，右侧为最终 MP4 解码帧。
- 完整页面证据：`outputs/design-qa/product-intro-guide-video-pilot-1440x810.png`，简体中文、亮色主题、第 2 页约 1.84 秒、自动播放已暂停。
- 在同一对照输入中检查后，标题、三模式上沿、清单文字、装置边缘、仪器读数、状态色和留白均保持一致；CRF 14 编码未造成可感知的结构偏移或明显文字糊化。

## 视频与交互核对

- 视频为 H.264 High、`yuv420p`、1050 × 750、30 fps、144 帧、4.800 秒、无音轨，文件大小约 182 KB。
- 画面使用软件现有 `HeatCapacityInstrumentScene`、原生模式控件和原生引导清单逐帧生成，没有重画界面、占位素材或近似模型。
- 视频进入页面时从 0 秒开始；暂停按钮会冻结视频，继续后从当前时间恢复；离开后再次进入会从头播放；播放结束保持最终画面并触发下一张介绍。
- 第二页正式试制路径实测 WebGL `canvas` 数量为 0；视频在第一张卡片期间即预载首帧，避免切页时临时创建三维上下文或首次解码。

## 换页性能核对

- 删除了轮播层每帧写入 React 状态的过渡逻辑，改为 360 ms 合成层淡入淡出与 8 px 横向位移。
- 干净首次流程、视频已预热状态下，第一页切到第二页共采样 41 个过渡帧；95% 帧间隔约 8.4 ms，最大帧间隔 25 ms。
- 进入和退出动画分别命中 `firstRunProductCardEnterNext` 与 `firstRunProductCardExitNext`，旧页与新页同步过渡，没有“新页先出现、旧页突然消失”的叠层跳变。

## Findings 与修复

- P2（已修正）：视频最初仅在切页时挂载，首次解码仍可能形成明显停顿；现改为隐藏预热并复用同一视频元素。
- P2（已修正）：旧轮播使用 `requestAnimationFrame` 连续触发 React 重渲染；现由 CSS 合成层完成过渡。
- P3（已修正）：开发捕获样式最初进入正式 CSS；现已拆到动态加载的开发专用入口，生产构建中不存在 `productIntroCapture`、捕获页类名或捕获桥接标记。
- 无未解决的 P0、P1 或 P2 问题。

## 自动验证

- TypeScript 无输出类型检查通过。
- 229 个测试文件全部通过；捕获入口拆分后首次体验 UI 定向测试再次通过。
- 生产构建通过，共转换 2135 个模块，视频作为独立哈希资源输出，构建产物策略检查通过。
- 固定预览 `http://127.0.0.1:5174/` 返回 HTTP 200；最终浏览器检查为 0 error、0 warning。

final result: passed

---

# Design QA — 产品介绍第二页“三种模式并行演示”

## 本轮范围与视觉依据

- 用户参考图：`<local-user>\AppData\Local\Temp\codex-clipboard-a4d9aceb-db3b-464a-8212-d2fd0841a9af.png`，用于核对页级标题、`02 / 03` 编号与第一页面的对齐关系。
- 模式画面参考图：`<local-user>\AppData\Local\Temp\codex-clipboard-2524323f-727c-4b19-8a9e-24e27280a243.png`，用于核对三维区域、模式控件、操作清单和设备视角。
- 最终实现截图：`output/design-qa/product-intro-modes-second-card.png`。
- 页头对照图：`output/design-qa/product-intro-modes-header-comparison.png`。
- 页面继续沿用第一张介绍页的无边框演示舞台：左侧为主标题，右侧为 `02 / 03` 与“学习方式”，三栏内部说明独立布置，没有新增外层卡片或重复说明区。

## 视觉与布局核对

- 三种模式横向等宽并列，分别显示模式名称、用途和独立状态，不依赖悬浮面板叠放信息。
- 每栏使用真实的绝热膨胀三维装置，并保留模式按钮、当前步骤和必要的任务进度；删除传感器预热 5 秒等不适合产品介绍的等待内容。
- 三个模型由同一个 WebGL 画布通过裁剪视口并行渲染，避免三个独立画布造成的启动延迟和资源竞争；三栏视觉尺度与照明保持一致。
- 模型画面适度放大，阀门、电源和压力调零等目标控件可辨认，同时保持页面标题、左右箭头、轮播进度与底部操作完整可见。
- 简体中文、繁体中文和英文均完成宽度核对；长文案没有横向溢出。亮色、暗色主题以及 900 × 600 窄窗口均保持内容可用。

## 动画脚本与交互核对

- 三栏在模型完成首帧加载后同时开始，避免鼠标先动、设备后出现；脚本总长约 5.8 秒，并按产品介绍节奏压缩操作间隔。
- 演示模式：鼠标从探索状态点击“演示模式”，依次展示开启电源、打开活塞阀、压力调零三个步骤。
- 引导模式：鼠标点击“引导模式”后，精准点击对应控件，右侧清单随操作逐项完成前三步。
- 自由模式：鼠标点击“自由模式”后自由尝试电源、活塞阀和气泵阀门，包含阀门反复开关，不要求遵循正式实验顺序。
- 页面暂停后，时间线、鼠标、设备状态和模型旋转全部冻结；继续后从原进度恢复。页面离场时三维状态仍在运动，并与淡出同步，不产生“先停住再翻页”的割裂感。
- 自动播放完成后进入下一张介绍页；减少动态效果系统偏好下关闭自动播放和脚本鼠标，仅显示稳定的最终状态。

## Findings

- 无未解决的 P0、P1 或 P2 问题。
- P1（已修复）：初版为三栏分别创建 WebGL 上下文，首帧加载慢且并行动画不稳定；现改为一个共享画布和三个裁剪视口。
- P2（已修复）：时间线曾在模型资源就绪前开始，造成设备出现时脚本已推进；现由共享渲染器首帧就绪信号统一启动。
- P2（已修复）：模拟鼠标的模式按钮和设备控件坐标与缩放后的画面存在偏差；已依据最终布局重新标定，点击环与目标位置一致。

## 自动验证

- TypeScript 无输出类型检查通过。
- 229 个测试文件全部通过，其中包含首次体验 UI 定向测试。
- 生产构建通过，共转换 2134 个模块，构建输出策略检查通过。
- 固定预览 `http://127.0.0.1:5174/` 返回 HTTP 200。
- 浏览器实测覆盖自动播放、暂停/继续、跨页淡化、减少动态效果、三种语言、明暗主题和窄窗口；控制台为 0 error、0 warning。

final result: passed

---

# Design QA — 产品介绍工作台模型持续旋转与跨页淡化

## 对照目标与证据

- 视觉基线：`output/playwright/product-intro-workbench-loaded-static.png`，用于确认工作台结构、模型尺度、字体、留白和工程软件色彩没有被本轮动效破坏。
- 实现证据：`outputs/playwright/product-intro-model-rotation-during-transition.png`，捕获第 1 页仍在淡出、第 2 页已经进入的同一过渡帧。
- 两张截图均为 2048 × 1025 px；浏览器 CSS 视口为 2048 × 1025，设备像素比 1，无密度缩放差异。
- 状态：简体中文、亮色主题、第一张介绍页自动播放；过渡帧中第一张页面透明度约 0.56，模型旋转进度约 0.917，第二张页面已经可见。
- 已在同一视觉输入中并列打开静态工作台基线和跨页实现截图进行复核。重点区域就是中央工作台与三维模型，因此无需额外裁切；全景尺寸已足以判断模型比例、页面叠化和标题层级。

## 五项视觉核对

- 字体与层级：标题、页码、工作台小字号和第二页正文继续使用现有字体、字重与行高；跨页叠化没有造成重影长期停留或文字裁切。
- 间距与布局：工作台仍保持原有 16:9 比例、水平居中、标题与演示区对齐；旋转只改变模型朝向，不改变画布尺寸或工作台分栏。
- 颜色与令牌：继续沿用工程软件的浅灰表面、深色正文和有限蓝色状态；淡化仅调整整体透明度，没有引入渐变、玻璃模糊或新颜色体系。
- 图像与资产：使用真实的 `HeatCapacityUltraInstrumentAsset` 三维装置，未用占位图、CSS 绘图或近似素材替代；旋转后模型边缘、透明容器和仪器面板仍清晰。
- 文案与内容：第 1 页和第 2 页既有文案保持不变；本轮没有增加解释性堆叠或占用垂直空间的新文字。

## 动效与交互核对

- 模型在文件载入后开始旋转，60° 行程使用 3.6 秒，平均速度约 16.7°/秒，较上一版更慢。
- 页面切换发生时模型尚未停止：实测跨页中旋转进度继续推进，第一张页面同时按 300 ms 受控透明度淡出，第二张页面同步进入。
- 自动播放暂停时，模型进度从 `0.267` 等待 1 秒后仍为 `0.267`；恢复 500 ms 后推进至 `0.437`，证明暂停与恢复没有偷跑或重置。
- 页面隐藏/浏览器节流时使用基于 `performance.now()` 的累计时间，避免依赖三维画布按需渲染帧率而出现卡住。
- 固定预览最终停在第 1 张、进度 `0.000`、暂停状态，便于人工从头体验。

## 比较历史与修复

- 第 1 轮 P2：模型旋转先结束，页面随后才切换，形成可感知的“停住—等待—翻页”。修复为延长并放慢旋转，使尾段覆盖页面切换时刻。
- 第 2 轮 P2：仅依赖 CSS 动画时，保留的旧页面在稳定节点上没有可靠更新透明度。修复为在轮播层以 16 ms 受控进度驱动 300 ms 淡化，同时用稳定键保留旧页三维模型直到过渡结束。
- 修复后证据：跨页中第一张透明度约 `0.56352`，模型进度约 `0.917`，页面类名为 `outgoing`，第二张状态已生效；无未解决的 P0、P1 或 P2 问题。

## 验证

- TypeScript 无输出类型检查通过。
- 229 个测试文件全部通过，其中包括首次体验 UI 定向测试。
- 生产构建通过，共转换 2133 个模块，构建输出策略检查通过。
- 固定预览 `http://127.0.0.1:5174/` 返回 HTTP 200；最新浏览器控制台检查为 0 error、0 warning。
- 浏览器实测模型旋转、跨页叠化、暂停、恢复和自动切页均通过。

final result: passed

---

# Design QA — 产品介绍工作台还原与脚本鼠标修复

## 本轮视觉真值

- 空工作台参考：`output/playwright/workbench-empty-reference.png`，直接捕获当前真实工作台的空文件状态。
- 实验工作台参考：`output/playwright/workbench-loaded-reference.png`，直接捕获当前绝热膨胀实验工作台。
- 产品介绍空状态：`output/playwright/product-intro-workbench-initial.png`。
- 产品介绍实验状态：`output/playwright/product-intro-workbench-loaded-static.png`。
- 四张图片均使用 2048 × 1025 CSS px 浏览器视口；空状态与实验状态分别在同一次视觉输入中并排复核。

## 结构与视觉核对

- 缩略工作台已恢复真实的四层骨架：顶部命令栏、文件工作区、控制台 / 输出、底部状态栏。
- 左侧栏宽度按真实工作台比例提升，并恢复“打开文件”标题、“文件 / 面板”分区、空状态提示、文件树和单击 / 双击提示。
- 空工作区恢复真实的文件标签栏、四个实验创建入口、打开实验入口及禁用缓存状态；绝热膨胀入口位于真实的 2 × 2 操作网格位置。
- 实验状态恢复文件标签、3D 预览 / 实时数据分栏、三种模式控件、折叠参数轨、实验文件树、数据卡片和真实三维装置。
- 字体、细边框、实体表面、低扩散阴影和有限状态色均沿用现有工程软件设计变量；没有引入渐变、玻璃效果或第二套视觉语言。

## 鼠标动画与交互核对

- 删除了把真实鼠标悬停和焦点停留解释为“暂停”的逻辑；当前仅主动暂停按钮和文档不可见状态会暂停自动演示。
- 在真实鼠标停留于演示区域时，`data-product-intro-demo-paused` 连续保持 `false`。
- 通过 Web Animations 时间轴采样验证脚本鼠标：0 ms 为 `(1495.09, 706.51)`，288 ms 为 `(1415.52, 579.87)`，576 ms 为 `(1335.95, 453.25)`，从首帧即开始连续移动。
- 修正最终点击纵坐标后，鼠标热点 `(1344.28, 440.35)` 位于绝热膨胀按钮边界 `x=1158.77–1427.67, y=423.92–453.02` 内，命中结果为 `true`。
- 左右切换、暂停 / 继续、底部上一步 / 下一步和循环播放逻辑保持可用。

## Findings

- 无未解决的 P0、P1 或 P2 问题。
- P1（已修正）：真实鼠标进入轮播区域会冻结脚本动画，导致鼠标只显示不移动。
- P2（已修正）：旧缩略工作台缺少控制台、状态栏、真实文件树和四入口空工作区，进入实验后的分栏比例也与主界面不一致。
- P3（已修正）：脚本鼠标原终点低于目标按钮约 2 px；已上移至按钮垂直中心附近并验证命中。

## 自动验证

- 首次体验 UI 定向测试通过。
- TypeScript 无输出类型检查通过；229 个测试文件全部通过。
- 生产构建通过，共转换 2133 个模块，构建输出策略检查通过。
- 固定预览地址返回 HTTP 200。

final result: passed

---

# Design QA — 产品介绍无边框演示舞台

## 本轮视觉真值与状态

- 视觉真值：用户标注截图与同轮文字要求共同构成目标；截图中的红框明确要求删除顶部重复说明，并取消把内容限制在外层卡片框中的做法。
- 源截图：`<local-user>\AppData\Local\Temp\codex-clipboard-4c7ce0e3-f1ea-4404-8cdb-c8aaff797764.png`，3072 × 1538 px；Windows 150% 缩放下对应约 2048 × 1025 CSS px。
- 浏览器实现截图：`output/playwright/product-intro-borderless-stage-after-wide.png`，浏览器视口 2048 × 1025 CSS px，应用内容截图 1824 × 980 px，设备像素比 1。
- 第 2 页双栏截图：`output/playwright/product-intro-two-column-card-after.png`，1440 × 774 px。
- 全景对照：`output/design-qa/product-intro-borderless-comparison.png`，左侧为源截图产品区，右侧为实现。
- 对照归一化：源截图裁取 2880 × 1376 px 的产品内容区，按比例缩放并居中补齐到 1200 × 645；实现截图缩放到 1200 × 645，最终并排为 2400 × 645。源图包含 Windows 缩放与较宽窗口，因此只对布局层级、占比和信息密度做判断，不做虚假的逐像素误差声明。
- 状态：简体中文、亮色主题、第 1 页动画起点、自动播放暂停；模拟鼠标保持可见。

## 五项视觉核对

- 字体与层级：删除重复的“产品介绍 / 认识实验工作台 / 说明文字”区；当前页只保留一行 20 px、500 字重标题，编号与主题标识使用现有数据字体。标题和演示窗口边缘已经对齐。
- 间距与布局：产品页改为“演示舞台 + 底部操作”两行结构；外层卡片边框、白色底板、圆角与阴影均已移除。工作台宽度由上一轮 742 px 增至 1050 px，线性尺寸增加约 42%，面积约增加 100%。
- 颜色与令牌：继续使用现有工程软件背景、文本、边框和操作色变量；未引入渐变、玻璃效果或新的装饰色。
- 图像与资产：工作台仍使用真实产品结构与现有三维实验模型；未添加占位素材，也未拉伸或裁切工作台画面，保持 16:9。
- 文案与内容：第 1 页只显示当前页标题；第 2、3 页将“编号和标题”放在左列、“说明和摘要”放在右列，避免四行内容纵向堆叠。

## 交互与响应式核对

- 左右箭头、键盘左右键、暂停/继续和底部下一步均保持可用。
- 页面切换继续采用短距离位移配合淡入淡出，没有恢复外层卡片移动效果。
- 已检查 1440 × 900 与 1000 × 700 浏览器视口；缩放后的工作台边缘、标题、箭头、进度和底部操作均完整可见。
- 浏览器控制台：0 error。

## Findings

- 无 P0、P1 或 P2 问题。
- P3（已修正）：首轮实现中标题行比工作台左右边缘各向外多 25 px；现已让标题行与 1050 px 演示窗口共用同一宽度，最终截图中左右完全对齐。

## 自动验证

- TypeScript 无输出类型检查通过。
- 229 个测试文件全部通过。
- 生产构建通过，共转换 2133 个模块，构建输出策略检查通过。
- 固定预览地址返回 HTTP 200。

final result: passed

---

# Design QA — 首次产品介绍「工作空间」卡片比例与节奏

## 本轮范围

- 只调整第 1 张「工作空间」卡片；第 2、3 张卡片的结构和文案保持不变。
- 目标是压缩卡片上部留白、放大工作台演示，并消除模拟鼠标出现后的无效等待。

## 对照材料

- 用户提供的修改前截图：`<local-user>\AppData\Local\Temp\codex-clipboard-5294d1b4-c16b-48eb-b85c-69ecd398c91c.png`
- 修改后卡片截图：`output/playwright/product-intro-workspace-card-after.png`
- 同画布左右对照（左：修改前；右：修改后）：`output/design-qa/product-intro-workspace-comparison.png`

## 捕获条件

- 浏览器视口：1440 × 900 CSS px。
- 应用内容保持 16:9 工作区；卡片实际尺寸为 920 × 472 CSS px。
- 修改后截图停留在第 1 张卡片动画起点，用于同时检查初始工作台和立即出现的模拟鼠标。

## 视觉与交互核对

- 卡片标题区由多行信息区压缩为 50 px 单行区域，仅保留黑色主标题；`01 / 03 · 工作空间` 已移至右上角。
- 工作台演示宽度由 630 px 增加到 742 px，并保持 16:9 比例；可视面积增加约 39%，同时完整保留工作台四周边界。
- 演示区域已占满标题下方的可用高度，左右留白对称，标题、序号和演示之间的层级清楚。
- 模拟鼠标在 0% 时即为可见，前 5% 开始移动，约 11% 完成点击；按 4.8 秒总时长计算，首次点击约在 0.53 秒完成。
- 工作台载入与三个面板的进入节奏相应前移，点击后不会继续停留在空状态。
- 真实浏览器检查中，第 2、3 张卡片未受到本轮专属样式影响；控制台为 0 error。WebGL 编译器仍报告既有的循环展开提示，不影响呈现。

## 核对历史

- 第 1 轮：完成标题区压缩、演示放大和鼠标时间轴前移。
- 第 1 轮浏览器复核：比例、边缘完整性、初始鼠标可见性和卡片切换均通过，无需二次视觉修正。

## 自动验证

- TypeScript 无输出类型检查通过。
- 229 个测试文件全部通过。
- 生产构建通过，共转换 2133 个模块，构建输出策略检查通过。
- 固定预览地址返回 HTTP 200。

final result: passed
